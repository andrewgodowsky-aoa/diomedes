# Product knowledge is indexed by the exact shipped bytes on every platform.
resources/product-knowledge/*.json text eol=lf

# Pinned upstream notice bytes; the collector verifies them by SHA-256.
licenses/codex-*.txt -text

# Sample businesses are rebuilt byte for byte and hashed; keep them LF everywhere.
resources/sample-businesses/** text eol=lf
