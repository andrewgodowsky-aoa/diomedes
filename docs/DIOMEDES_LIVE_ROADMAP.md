# DIOMEDES LIVE ROADMAP

Roadmap version: 2026-09-25.2
Last reconciled: September 25, 2026
Product: Nectovia (named Diomedes until 2026-09-22)
Company direction: Diomedes Systems
Cloud canonical: 1bRhz3zQPXOYuVlt95U1EIkz7pcm1dtSsoBvkDrLR3zE
Repository mirror: docs/DIOMEDES_LIVE_ROADMAP.md

## 1. Authority, current entry point and preservation

Andrew's newest explicit decision governs intent; docs/DIOMEDES_CORE_PILLARS.md governs durable constraints; this roadmap governs strategy and sequencing; docs/DIOMEDES_PROJECT_MEMORY.md governs definitions; current source and fresh verification govern implemented reality. An assistant narrative, documentation approval or roadmap checkbox is not execution evidence.

This checkpoint records Andrew's September 19 instruction to publish the Automations plan in GitHub and Drive and remove stale active guidance. It consolidates BUS-10 and OPS-08–OPS-10, preserves the September 17 model-aware harness amendment (MH-1), and separates current requirements from older release, test and worker-assignment statements. That checkpoint did not amend the Core Pillars; a separate owner decision later the same day did (Pillars 2026-09-19.1, Pillar 07 only), together with the pricing decisions in section 7. No application implementation or deployment is authorized or certified by this documentation publication.

**2026-09-23 checkpoint: the Nectovia rename.** Andrew named the product Nectovia on 2026-09-22; the Core Pillars carry it as patch 2026-09-22.1, and no pillar's meaning changed. It landed as display text only: the public site at main f28eb9f (live on diomedes.net, Cloudflare version 88ec3f83-8c60-4445-820d-3419f5374942) and the app at main 1123a14, which also carries the Nectovia Console skin, the model-artifacts panel and its hardening. The first release to carry it is 0.1.8, packaged after routing wave2 lands. Identifiers are unchanged. The remaining name decisions are listed in docs/product/2026-09-22-nectovia-skin.md and, for the site, in its docs/nectovia/2026-09-22-names.md. Model artifacts v2 (the decisions in docs/product/2026-09-22-model-artifacts.md section 5) was approved for implementation on 2026-09-23; the options taken are recorded with it when it lands.

**2026-09-24 checkpoint: current product, routing and releases.** This reconciles the repository and cloud copies, which had diverged: the cloud carried the NC-2026-09-22.1 commercial text, the repository carried the Nectovia rename. Both are kept, and Andrew's later decisions replace what they contradict. Released: 0.1.8 (first Nectovia build, 2026-09-23), 0.1.9, 0.1.10 and 0.1.11 (latest, 2026-09-24, build a492c42, release PR #70). The Workbook is removed (PR #56 f926018; its retired settings keys are refused, #66); there is one Console with two views, Conversation (the prompt and threads; the default for new people) and Architect (the full workspace) (#59). The History screen and the Sample project entry are removed (#57); history is still recorded, and a restore UI would be a new owner decision. Artifacts v2 is complete (#44, #46, #48, #49, #61). Tier routing and job caps follow section 7. The product site is nectovia.diomedes.net and diomedes.net is the Diomedes Systems company site. The Automations Milestone A work order is approved (section 2). App main at this checkpoint is 559a1ab; site main is 45bbd96.

**2026-09-24 overnight checkpoint: source landed, nothing released.** The overnight sprint ran bounded lanes on 2026-09-24, each with a record under docs/implementation/2026-09-24-*.md; docs/implementation/2026-09-24-overnight-sprint-summary.md lists every lane with its Linear issue, pull request, review and proposed defaults. App main is at 4395331 and carries batches 1 to 5: batch 1 (PR #86, merge 1b70c2e), batch 3 with batch 2 inside it (PR #97 carrying PR #95, merge f13be72), PRs #74, #75, #76 and #82 merged directly, batch 4 (PR #106, merge 8d20fb9: H02, remembered approvals for Codex proposals, H09, H12, H13, H15, H21, P06 and CD-05) and batch 5 (PR #107, merge 4395331: H03 and H05). Batch 6 is PR #119 and is landing: H10, H14, P04, independent reviews C, D and E with their fixes, and an H03 test fix. Reviews C, D and E accepted every feature they covered with fixes: H08, H02 and H18 (C); H12, H13, H15 and H17 (D); P05, P06, Automations B, D5 for Codex proposals, H09 and H21 (E). Those features are implemented in source and independently reviewed, and their review fixes arrive with batch 6. Review F of H03 and H05 is still running (PR #116). Also still running: H16 (PR #118), P07 (PR #115), H20 (PR #117), DIO-107 (bugfix/dio-107-two-window-load) and the H13 delegate sandbox (feature/h13-delegate-sandboxes). This is source, not a release: the latest release is still 0.1.11, no packaged build carries this work, and no lane has live-provider, hosted, packaged-installer or device proof. Where this roadmap says implemented, it means implemented in source with the tests its record names; live, hosted, installer and device acceptance stays open unless a sentence says otherwise. Anything that was Andrew's to decide took the most conservative option and is an open question in QUESTIONS.md (O11–O42), except where his 2026-09-24 decisions, below, answered it.

**2026-09-25 checkpoint: Andrew's decisions of 2026-09-24 on P06, H13, H15 and team mode.** They are recorded in docs/reference/DECISIONS_2026-09-24.md (PR #108) and applied here, in project memory and in QUESTIONS.md R10–R13. P06: readable diffs of recorded versions, per-change keep and review comments are Core; the Software Engineering pack keeps Git, syntax highlighting, unified and split code views and LSP, and decision 13 now says so. H13: a finish that is not Verified leaves the task in Review. Delegates will run in sandboxes, each an isolated working copy under H12 containment, and hand back a recorded change set that the parent may apply inside its own grant; anything else waits for a person. The limits are depth at most 2 and at most 4 delegates per run, running in parallel, with budgets carved from the parent's remaining budget. Delegate routes come from the project's H09 profiles, and the loop's attribution rules stand. The sandbox is H13's next slice, in progress on feature/h13-delegate-sandboxes and not landed; until it lands, delegates stay read-only as built. H15: the thresholds 3/4/6, 4/5/8 and 80/90 percent are accepted, and a project may tighten them but never loosen them (no setting for that exists yet). So are the correction bounds, folder-level scope, and an immediate pause on an unapproved write outside scope. A queued correction may start the next run without a person; it shares the origin run's bound, is admitted with no wider permission, is labelled as supervision and is cancelled by a pause or a Stop. One escalation is raised per task and issue, supervision is always on, and the instruction-drift reading stands. Team mode: every helper Diomedes dispatches works in a sandbox by default, with no more authority than the intersection of its parent's grant and its declared scope. Where it can, the system lets a helper work somewhere safe rather than refusing the work. Cloud synchronisation of these decisions is pending.

Read this entry point first. Detailed non-conflicting requirements remain incorporated from the previous bodies, preserved (unaltered except that retired Diomedes price figures were struck on 2026-09-19) at docs/reference/DIOMEDES_ROADMAP_PRE_AUTOMATIONS_2026-09-19.md and docs/reference/DIOMEDES_PROJECT_MEMORY_PRE_AUTOMATIONS_2026-09-19.md. Their historical statuses, test counts, release pointers, 'next' instructions and named worker assignments are not current instructions. Earlier requirements also remain in docs/reference/DIOMEDES_ROADMAP_BASE_2026-09-09.7.md and docs/reference/DIOMEDES_PROJECT_MEMORY_BASE_2026-09-10.2.md where not superseded by current decisions. Retrieve relevant detail rather than injecting every historical document.

Pre-reconciliation cloud snapshots: roadmap https://docs.google.com/document/d/18I5Y384mPcbT70AuH-N3wZA8cFodLq7xxczfAcC96bk/edit ; memory https://docs.google.com/document/d/1rDyz3L4RqBEiZM39-nuk-8AZjs3UYkAfjQnSMqDRAfo/edit . These preserve the distinct cloud and repository histories; neither older copy replaces the other.

Refresh cloud revisions before authorized writes, use requiredRevisionId, preserve concurrent edits and keep cloud/repository decisions materially equivalent. F:\Achilles and F:\Diomedes planning caches are not additional authorities. Existing coordination claims, prerequisite reviews and active work orders govern execution; a historical handoff never reassigns a worker.

## 2. Automations — Milestones A and B implemented in source; C–D planned

Canonical focused specification: docs/product/2026-09-19-automations.md, version 2026-09-19.1. Cloud counterpart: https://docs.google.com/document/d/1iOf_-0c1ft5w-KhJpp1eOXLMGzo8QVYHWQKJg9YYp5o/edit . Delivery/review mapping: docs/implementation/2026-09-19-automations-work-items.md. This is the current planning authority for BUS-10 (owner workflow visibility), OPS-08 (recurring execution), OPS-09 (host availability) and OPS-10 (notifications); do not start a duplicate initiative from their older short descriptions.

Milestone A work order: docs/implementation/2026-09-24-automations-milestone-a-work-order.md, version 2026-09-24.2 (PR #69). Andrew answered its decisions on 2026-09-24. D1: Run once executes the weekly brief through RunService as a model-free weekly-brief harness capability, with one new trigger-occurrence record holding the command identity and pinned configuration. D2: saving the draft needs no per-run approval; the person keeps or undoes it afterwards. D3: a missing configured source stops the run before writing, shown as Waiting for data. D4: Automations moves into the Nectovia group and the home row opens it. D5 (remembered approvals) is recorded in project memory; Milestone A builds none of it. Slices A0–A7 are implemented (PR #85, on main through PR #86, 1b70c2e; docs/implementation/2026-09-24-automations-a.md) and independently reviewed with one fix, the label read from the latest run (PR #91, on main through PR #97). QUESTIONS.md R9 records D1–D4 as settled in code. The packaged Windows journey remains open.

Automations is a first-class destination inside the existing Console for Personal and Business. It shows what is configured, what remains manual, purpose and limits, accountable owner, authorized project/location scope, execution host, trigger and next occurrence, source freshness, live stage progress, last verified result, missing coverage, attributable usage and actionable Needs. Use plain descriptive labels and the current readable Console visual system. Technical details remain inspectable, not mandatory.

An automation is an ongoing recurring/event-driven responsibility, not an Agent or individual run. Versioned definitions and durable trigger occurrences feed existing task admission and Harness RunService. They do not introduce another executor, run-state owner, permission system, artifact store, approval inbox or scheduler per pack. Thread, Auto To Do, Team, Files and Automations reference the same durable work. Installing a pack does not enable a schedule or grant authority.

Keep configuration, present eligibility, authoritative execution state, verified result and human attention distinct. Enabled does not mean able to run. Finished does not mean verified. Saved does not mean delivered. Pause future runs does not stop an existing run. A lost response after an external effect requires reconciliation, not a blind retry. Unknown health, missing sources, uncertain usage and unsupported control guarantees must be visible. Core safety and truthful visibility are shared, not premium safety features.

The source audit is pinned to app main 448448683458c2ef813035f344baa79a4c6817bd. At that snapshot, client/console/types.ts has no Automations ShellView, shared/packs.ts keeps schedules inactive and supports manual starts, and server/harness/run-service.ts explicitly is not a scheduler. The existing weekly brief produces a source-linked saved draft, not outbound delivery. These are bounded source findings, not a test of unseen worktrees or proof of current production readiness. At main f13be72 the Console has the Automations screen, the weekly brief runs through RunService as a model-free procedure, and a schedule runs only while Nectovia is running on its one assigned computer.

Delivery is dependency-ordered, not a new worker assignment:

- Milestone A / BUS-10 foundation — implemented in source 2026-09-24: the Console Automations screen lists the active business's weekly brief as **Manual — not scheduled**; Run once admits it through a durable TriggerOccurrence into the `weekly-brief` RunService capability (no model call); a missing source stops before writing as Waiting for data; non-members see nothing; a restart rebuilds the same records. The packaged Windows journey is still required for acceptance. Production organization aggregation still requires verified tenant controls.
- Milestone B / OPS-08 + OPS-09 (+ OPS-10 in-app) — implemented in source 2026-09-24 (PR #96, on main through PR #97; docs/implementation/2026-09-24-automations-b.md; DIO-91–DIO-93): a stored, versioned AutomationDefinition holds a daily or weekly local-time schedule in a named IANA timezone; an owner or admin turns it on as a recorded act; one small scheduler, which owns only a clock, admits each due slot through the same admission as Run once, with a slot identity derived from the automation, revision and slot. DST, missed slots (all recorded, only the latest caught up within 2 hours), pause/resume, overlap, fire-time revalidation (host, enabler authority, signed-in person, setup revision, spending bound) and restart recovery are proved by tests; missed, skipped, blocked and failed slots raise one in-app attention item per issue, also in the project's Needs you. One assigned host; no background service, multi-host, push, email or phone. Review E (docs/implementation/2026-09-24-review-e.md, PR #113; fixes in batch 6) accepted it with fixes: a slot that fell due just before a pause, edit or resume is still admitted or recorded; a missed run names when the computer was really last seen; and a calendar day a zone skipped whole makes one slot, not two. The packaged Windows journey is still required for acceptance.
- Milestone C / supported events + OPS-10: named authenticated connector/event paths, ordering/deduplication/reconciliation, scoped notifications beyond in-app attention and production multi-location authorization after their identity, credentials and Trust prerequisites are proved.
- Milestone D: governed pack templates, reviewed plain-language configuration, supported remote operation and baseline-backed value reporting. Multi-host takeover requires separate ownership and destination-effect proofs.

The 36-case matrix A01–A36 is required acceptance coverage, not a passed test suite; the A and B records name the cases their tests prove, and the rest stay open. Milestone B's schedule defaults are proposed, not approved (QUESTIONS.md O12); in particular a local time that does not exist runs when the clocks jump, where the specification proposed skipping it. Cloud inference alone never establishes an always-on execution host; reliable offline alerts need an independent authorized observer. No generic canvas, arbitrary connector support, unrestricted remote shell, automatic payer/cloud fallback or invented ROI is implied. Existing Field Readiness milestones and optional SDKO status remain unchanged.

## 3. North star and shared product

A genuine general-purpose, Diomedes-led agent with its own harness/runtime and one human Console: objective → plan → context/routes/tools → bounded delegation → monitoring/correction → authority where needed → verification → durable result. No routine babysitting is the destination, not a claim that every model or workflow is already safe unattended.

One configurable product serves business, software, learning, personal and technical work. Industry differences use capability packs, workflows, schemas, mappings, rules and connectors, not product forks. Restaurants and construction/remodeling are validation contexts, not the product's limits. Prefer deterministic mechanisms where reasoning adds no value; coordinate existing systems before claiming to replace them.

Personal remains a strong free self-managed local/BYO harness. Business adds organization identity/configuration, maintained Agents/Teams/rules, supported connections, managed inference, governance and operational services on the same Core/Console. Basic safety is shared. Person, workspace, organization, membership, configuration, entitlement, usage account and execution authority remain separate. Business intake is authorized-owner/admin-only in a selected Business workspace; ordinary invitees do not restart company setup and Personal never receives it.

Agent is a versioned Diomedes-owned worker contract; Teams compose Agents. Mode → Agent → Model → Effort is a conceptual control model, not a rigid execution pipeline. Direct external-engine work stays first-class and truthfully attributed. Native supervision does not require proprietary weights. Configuration follows facts → candidate → validation → review/rehearsal → versioned activation; it never grants authority or revives revoked access or spent credit.

## 4. Model-aware harness and task execution styles — MH-1 preserved

Approved direction, amendment MH-1. Diomedes owns a true harness: model-facing tool design, context assembly, typed action validation, execution policy, observations, bounded correction and outcome verification. Adapters provide model or external-agent access beneath that ownership. This amendment records requirements; it does not certify their implementation.

Personal technical work, software engineering, learning and non-business projects are first-class uses of the same harness as business operations. Execution quality, benchmark performance, token and cost efficiency, latency and recovery reliability are product requirements for both Personal and Business. Evaluate quality and safety alongside efficiency; a shorter answer or skipped verification is not an improvement. Earlier rationale declining a coding-loop competition does not limit this approved performance work or prohibit reuse of suitable components.

Select and record an execution style per task: a Diomedes-owned model/tool loop for full control of the tool interaction, or a bounded external-harness task using only the controls that route demonstrably exposes. Record exact model/provider/route/payer, capability revision, tool behavior profile and profile digest. A change occurs at an explicit admitted checkpoint; it never silently changes authority, payer or data policy. Direct external-engine use remains first-class and truthfully attributed.

Projects support a lead Agent backed by a selected model that plans, distributes bounded tasks, reviews worker results and integrates a verified outcome. Project-only single-agent work, Team view on its own, temporary workers and Project-plus-Team work are all supported product requirements. Projects retain durable context/files/tasks/history; Teams organize workers. Both views share the existing task/run/artifact records, and all effects still need an explicit authorized work target. H13/H14 own orchestration/delegation; H09/H18/H19/H20 own profiles, scoped context, views and comparative evidence. Measure single-agent versus lead/worker quality, total tokens/cost and latency including coordination; delegation is not automatically more efficient. This coordination experience is planned, not certified by the documentation amendment.

H14 slice 1 (DIO-19, PR #112, in batch 6, PR #119, landing; docs/implementation/2026-09-24-h14-teams.md): an H13 lead loop admitted with a team hands bounded tasks to workers and may ask a read-only advisor. Workers run one level deep, at most 3 at once and 6 per lead run. Each worker is its own run with an explicit budget (turns, reported tokens, wall time), a read scope inside the lead's, its Agent and optional H09 profile pinned at admission, and no more than the lead's authority. Workers only read, so every effect stays the lead's, through H12 and a Need. Every handoff is appended to a durable ledger and shown in the Team view with its budget, its answer and verification through the lead's declared checks. A worker that fails stops its lead, and H08 Retry reruns only that worker. On the fixture and a stub route, delegation was slower and larger when each sub-task was one call, and faster and smaller when each was several. Under Andrew's team-mode decision (QUESTIONS.md R13), workers will work in sandboxes once H13's sandbox lands. How H14's limits and budgets meet the H13 limits is QUESTIONS.md O36. H14 is not live-proven and not yet independently reviewed; worker-proposed changes, per-handoff acceptance and a Console start remain open.

Harness status at 2026-09-24, all in source, proven with fixtures or mocked providers and never against a live route. H13 slice 1 (DIO-18, PR #104, on main through #106) is a Diomedes-owned plan → act → observe → finish loop (`diomedes-loop`). Every phase is a durable run step, bounded by turns and run budget with a truthful stopped outcome. It acts only through registered tools under Trust, with H11 instructions and H18 accounting. It has one-level read-only delegation under a handoff envelope, with Stop propagation and restart recovery, and its finish reads done only when H17 verifies the person's declared checks. It is proven on the fixture route and, with mocked transports under the real SDKs, on AWS Bedrock and Google Vertex AI. Andrew's 2026-09-24 decisions (section 1; QUESTIONS.md R11) keep an unverified finish in Review and replace read-only delegation with sandboxed delegates that return change sets; that sandbox is the next slice, in progress and not landed. Codex delegation, a Console start control (QUESTIONS.md O23) and correction after failed verification remain open. H15 (DIO-20, PR #105, on main through #106): Diomedes supervision reads each live run's records and detects scope drift, no-progress loops, budget burn, instruction drift and verification regression. Each finding climbs a declared ladder: a note, then a bounded correction through H08 Steer or Queue, then an H08 Stop and an ordinary Need that only a person answers and that grants nothing. Continue on provider routes waits for a route that wires Resume. H16 stream-time triggers are separate (PR #118, running). Andrew accepted H15's thresholds and defaults on 2026-09-24, and answered its open queued-correction question (QUESTIONS.md R12); a project setting that tightens the thresholds does not exist yet. H17 first slice (DIO-22, PR #87, on main through #97): four projected verification states bound to exact output digests, deterministic file checks and an optional separate reviewer. Declared project commands are recorded and never run, and verification runs automatically only at the end of an H13 loop. H18 slice 1 (DIO-23, PR #89, on main through #97) covers the model-API conversation driver only: per-turn context accounting reconciled against provider usage and cache reads, recency-plus-lexical history selection with a truthful marker, a deterministic application-written compaction record and a recorded byte-stable prefix. Declared context windows, explicit provider caching and Work-turn accounting remain open. H09 (DIO-14, PR #100, on main through #106): exact-model Agent profiles and routing preferences, with fallback off by default and immutable resolution pinned at admission, for guarded Build/Fix runs only. H19 was not part of this sprint; H20 (headless evaluation and an all-route acceptance matrix, PR #117) is running.

Independent reviews of these lanes (2026-09-24; the fixes ride batch 6, PR #119, landing):

- Review D (docs/implementation/2026-09-24-review-d.md, PR #110) accepted H12, H13, H15 and H17 with fixes. The loop's actions now go through H12's mediated dispatch. A delegate reads only what every cloud route its answer reaches may see. Supervision acknowledges a continued issue only for the run it resumed, and reads an instruction prohibition from its own clause. A verification binds every file it judged, and a saved version is not counted as a run's output. Still open from review D: refusing a new start while a supervision escalation is open (QUESTIONS.md O41), charging a delegate's spend to its parent's job cap (which R11's carved budgets require), and a job-cap stop that parks a call it never sent.
- Review C (docs/implementation/2026-09-24-review-c.md, PR #109) accepted H18 with fixes: a message that does not fit the history is omitted and summarised, never recorded as included and then cut.
- Review E (docs/implementation/2026-09-24-review-e.md, PR #113) accepted H09 with fixes. A routing list never replaces an Agent the person chose; that was a P0, because it could widen authority. A list never routes a team member's wake, and an unreadable profiles file refuses the run instead of falling open.

Each of these is implemented in source and independently reviewed; none is released or live-proven.

SDKR remains the Vercel AI SDK direct-model operation bridge under the existing ModelAdapter. H01 owns external route contracts; ACP remains the preferred supported external transport. Optional HarnessAgent experiments stay in SDKO and do not replace either contract or introduce a second runtime.

Close the explicit tool-behavior gap in H09, H12 and H13: version and test the model-facing schema or grammar, instructions/examples, parser, result feedback and recovery policy together. Translate responses into the same canonical typed actions, validate them, then authorize before effects through H12 and the existing recorded writer. Software edit formats belong in the Software Engineering Capability Pack; non-coding typed operations use the same Core boundary. Compare model/profile combinations on verified completion, malformed calls, stale edits, no-progress loops, corrections, latency and usage/cost before adopting a default. No universal performance gain is claimed.

A sandbox bridge is transport into an execution boundary, not the boundary itself. Verify filesystem, network, credentials and process containment; host-side tools still require Trust checks. Model inference location, control protocol and tool execution location are independent choices. A local app-server can serve a cloud model and is not rejected solely for being local. Prefer the mediated sandbox execution path for effectful work; refuse or narrow work when the necessary containment or external controls are absent.

Reuse suitable proven mechanisms, including evaluating oh-my-pi tool formats, subject to compatibility, licensing and measured benefit. Diomedes retains Runtime, Trust, Store and History ownership. The implementation and acceptance details are in docs/product/2026-09-17-model-aware-harness.md and shared/MODEL_AWARE_HARNESS.md in the unified execution package. Existing prerequisite reviews, Field Readiness milestones and optional SDKO status remain unchanged. Those references describe the existing MH-1 package; their presence on a local branch must be checked rather than inferred from this index.

## 5. Runtime, Projects, Files and packs — retained integration constraints

Diomedes contract first; ACP is the preferred supported external transport. H01 remains the common external-route seam and server/harness/RunService the durable execution spine. Text adapters and native app-server paths must conform rather than become parallel runtimes. Text-only is a narrow single-turn support mode, not the universal agent interface. Supported session lifecycle, streaming, tools, approvals, steering, cancellation, resume/fork, artifacts and evidence must be declared honestly. Codex, Claude Code and OpenCode stay native unless a pinned ACP route proves equal or better capabilities; the shared ACP client supports configuration-driven external routes. Preserve the Cursor/Devin conformance requirements without treating a past proof sequence as a current worker assignment. Compatibility questions remain in QUESTIONS.md O8.

Work controls and routes at 2026-09-24, in source. H08 (DIO-13, PR #93, on main through #97): Steer, Queue, Stop, Resume, Retry and Fork are six distinct Work commands under work-control revision 2026-09-24.1, each with a command identity and an append-only control receipt; availability is read from each route's adapter contract plus the drivers wired to Work runs; Retry is blocked while an effect may already have happened, Resume never runs with wider authority, and Fork never changes its origin. On main, real routes offer Queue and Stop, and Retry on every non-sample route. H02 (DIO-7, PR #98, on main through #106) wires Codex: a ChatGPT Work run records its app-server thread before its turn and probes thread/resume, thread/fork and turn/steer; where the installed Codex answers, the Console offers Steer, a Resume that continues the thread and Fork, and otherwise a labelled Queue, a Resume that says it started fresh, and no Fork. H02 is proven against a fixture app-server only. Live acceptance on Windows with the pinned build remains, including the isolation proof for a kept thread and whether Codex accepts the isolation fields on resume and fork. Review C (docs/implementation/2026-09-24-review-c.md, PR #109; fixes in batch 6) accepted H08 and H02 with fixes:

- A generation Stop is credited to the engine only where the contract says the engine interrupts natively.
- Retry and Resume are blocked by an uncertain effect anywhere in the run's lineage.
- Stop is never refused for receipt capacity.
- Only a thread-not-found answer may start a Resume fresh or refuse a Fork; other Codex errors fail the run.
- Resume and fork carry the same isolation fields as a new thread.
- Native Fork needs resume as well.
- A stopped run is not resumed twice.

Still open from review C: Resume and Fork hold the service lock while Codex starts (QUESTIONS.md O40).

H03 (DIO-8, PR #92, on main through batch 5, PR #107; docs/implementation/2026-09-24-h03-claude-live-controls.md) gives the Claude Code native session:

- a truthful continuity read (live, resumable, or start again with its reason);
- host-queued steering, never mid-turn;
- Stop with a graceful interrupt, a bounded grace and a recorded process-tree kill;
- restart reconciliation that never shows a ghost Running;
- capability-driven controls on the Diomedes page.

Project threads on Claude Code still use the single-turn path (QUESTIONS.md O38).

H05 (PR #90, on main through PR #107; docs/implementation/2026-09-24-h05-cursor-acp-sessions.md) gives Cursor and Devin one kept ACP conversation over the shared ACP client:

- the ACP session id is saved before each prompt and continued by session/load where the engine advertises it;
- a restart mid-turn is reconciled and never resent;
- Stop is session/cancel with a bounded wait and a recorded end of the process tree;
- plan approvals and out-of-scope fetch asks become Needs a person answers.

H05 is an API route, not a Console conversation route (QUESTIONS.md O39).

Both have fixture, conformance and browser proof only. Live acceptance against Claude Code 2.1.252, Cursor 2026.08.11 and Devin 3000.10.23 remains, as does packaged qualification. Independent review F of both is running (PR #116).

H04 (DIO-9, PR #82, main 9bd5147, reviewed with fixes in PR #88): an OpenCode kept session with a durable native session id, host-queued steering (never mid-turn), Stop with confirmed idle, resume after restart with a stated fresh-start path and fork, through the shared native conversation driver; fixture and conformance proof only, and no Console surface until OpenCode is admitted as a conversation route (CD-01 Decision 5, QUESTIONS.md O19). H07 (DIO-12, PR #78, on main through #86, reviewed with a consent fix in PR #91): per-project opt-in automatic start of Ready tasks (off by default) through the Work start admission, least-recently-served across projects and FIFO within one, limits 1 per project and 2 overall with manual work counted, durable project and global pause, and crash-safe claims; a service-route task starts automatically only as an exact re-send of a request the person already confirmed. The Automations scheduler does not feed the Ready queue: it owns only its clock and uses the automation admission.

The AI SDK direct-model plane does not absorb external-agent sessions. H12 and the recorded writer mediate effects; observed route capabilities become diomedes-enforced only after native bypass/containment is proved. ACP-reported model identity is observed, not enforced. Team handoffs reuse the existing team MCP host through supported transport or a Diomedes-side proxy, never laundering authority.

H12 (DIO-17, PR #102, on main through #106): every host-executed harness tool declares input and output schemas, an effect class, its permission and approval policy and the targets it may change, and an incomplete declaration is refused at registration. Before a tool with an effect runs, its intent is written durably in the run's step record and its outcome is recorded against it; an interruption between the two leaves the effect uncertain, it is never re-executed on a guess, and Retry and Resume refuse until it is reconciled on evidence. Tool paths go through one containment funnel and a tool process runs without a shell, with a minimal environment, bounded output and a timeout that ends its tree, proven by a table-driven attack matrix (junction cases on Windows CI). External engines' own tools remain observed, not mediated; a person has no Console place to reconcile an uncertain effect yet. Review D accepted H12 with fixes (PR #110, in batch 6). The effect.* events joined the stream vocabulary. A contained write whose landed file is not the one written is now uncertain rather than failed, and the spawn guard refuses every key-named variable.

A Project is a durable, general-purpose container for context, Threads, Tasks, files/artifacts, rules, permissions and evidence; it is not a repository synonym. The native Agent may work across the Project. One Core Files surface serves business documents and software; the Software Engineering pack adds IDE-grade affordances in that same Console, not another application or file authority. Reuse Project, DocumentInfo, DocumentContent and History. See docs/product/2026-09-10-project-files-and-agent-overview.md and docs/product/2026-09-10-capability-packs.md. P05 (DIO-31, PR #80, on main through #97) adds drop and paste into Files through the one recorded write (pictures, PDFs and workbooks as exact bytes, a person's own writes only), byte-sniffed read-only previews (pictures, CSV/TSV and an XLSX first sheet as bounded tables; a PDF is described, not rendered), Thread attachments carried as the message's selected text sources, and durable file identity projected from History; an in-app PDF viewer, open-externally, binary restore and sending pictures or PDFs to an engine remain open. P06 (DIO-32, PR #103, on main through #106) adds readable diffs of recorded text versions and run changes, per-hunk keep as one base-hash-checked recorded write with hunk-level History evidence, and durable review comments that reach a run only as a previewed follow-up; an H17 result turns uncertain when a partial keep changes verified bytes. There is no three-way merge and no Git-backed or highlighted diff. On 2026-09-24 Andrew placed readable diffs of recorded versions, per-change keep and review comments in Core. The Software Engineering pack keeps Git, syntax highlighting, unified and split code views and LSP (QUESTIONS.md R10; decision 13). Review E (PR #113; fixes in batch 6) accepted P05 and P06 with fixes:

- A small workbook can no longer hold the store lock for minutes.
- A version a message sent stays in History after a quick second edit.
- A GIF is held to the pixel limit by its largest frame.
- A change whose run is still writing takes no comment yet.
- A comment chosen twice is sent once.

Still open from review E: version order after an A→B→A edit, hunks that differ only in line endings, and whether a version comment rides with any task that changed the file (QUESTIONS.md O42).

Packs compose tools, Agents, rules, context, workflows and relevant UI, not merely prompts. They do not introduce parallel Runtime/Trust/storage or grant authority when activated. Load only relevant toolsets. Discovered instruction files remain source/version/scope-tracked, inspectable standing guidance through the existing context/rule path. The historical HAR-01 wording disagreement is not resolved by this automation amendment; use its focused implementation record and QUESTIONS.md rather than claiming instructions were never delivered. H11 (DIO-16, PR #75, main 5d73f1f, reviewed with fixes in PR #88): instruction files in nested folders are discovered under depth, folder and file bounds through the path guard and govern only work on documents inside their folder; precedence is authority first, then the nearer folder, then AGENTS.md before CLAUDE.md; each run records every file's scope, precedence, sha, bytes and whether it was sent or why not, and the Console inspector under `Project instructions loaded` shows that record. Discovery is not re-run at every run start, and bound hits are not recorded. P01 and P03 (DIO-27, DIO-29, PR #81, on main through #86, reviewed with fixes in PR #88): every pack has a versioned manifest with typed contributions, requested permissions that Trust still decides, semver dependencies and a content digest; packs install from the bundled catalogue or a local folder verified by digest and are activated per Project, updated, rolled back and uninstalled as recorded, append-only, crash-safe operations. Only the Software Engineering and Small Business packs' contributions are run; other installed packs are declared and listed, not run. P02 (registry and GitHub acquisition) is not started.

P04 (DIO-30, PR #114, in batch 6, PR #119, landing): turning a pack on registers an index of its contributions (names, one-line descriptions, triggers, a digest per body) and loads no body. A body loads through the run's pin only when it is used:

- a Small Business playbook, when the person chooses it or a model-API Ask/Plan turn asks for it by its index entry;
- a playbook panel, when a person opens it.

Each loaded body is checked against its registered digest, and a mismatch is refused. The load is recorded in History with the pack version and digest. Turning a pack off unloads it. A run already admitted keeps the version it pinned, which composes with rollback. For the twelve Small Business playbooks, the index costs 2,490 bytes (about 623 tokens) against 27,016 (about 6,754) for every body. Installed packs' tools, Agents and context are indexed and loadable but have no Runtime consumer. Its defaults are QUESTIONS.md O37.

H10 (DIO-15, PR #111, in batch 6): Diomedes proposes one line for a loaded project instruction file when the record shows the same correction three times. The corrections it counts are a run's write a person undid, H15 scope drift a person stopped or redirected, and the same review comment. Each proposal comes with its evidence, the P06 diff and a replay of the recorded runs through H15's instruction check (improved, unchanged, worse or not evaluable). It never writes on its own, and a decline holds until new evidence arrives. Each applied revision and each one-step rollback is a record in an append-only SHA-256 digest chain. It is not an HMAC, because there is no local key material. The record is attributed to the person who approved or rolled it back, H11 discovery and delivery follow the change, and open conversations are not reset. Gaps: there is no model-backed evaluation, prose lines cannot be evaluated, a person's direct edits are not chain records, and the chain detects a wholesale rewrite but cannot prevent one. Its defaults are QUESTIONS.md O35.

Neither P04 nor H10 has had an independent review. P07 (the Software Engineering pack's Git view, worktrees and mediated declared commands, PR #115) is running.

Human statuses are pure projections over authoritative tasks, runs, Needs, reviews and evidence. 'Recently' is a display window, not retention. History/receipts/attribution may not be pruned by a blunt setting; deliberate archival/deletion policies remain required. Resolved paths, including Windows short-name aliases, are checked through the existing path guard. H21 (DIO-26, PR #101, on main through #106): durable files belong to registered, versioned families; seven are read through one migration framework that keeps a byte-for-byte backup and refuses a newer file without changing it, and the rest are listed with their gaps. A twelve-boundary crash matrix, `npm run perf:gate` with recorded baselines and the completion journey (`npm run test:journey`) exist; neither the perf gate nor the journey runs in CI yet, and SSE fan-out of the full state is the measured slow path. Review E accepted H21 with fixes (PR #113, in batch 6). A prepared write left by an interrupted save is now held to the same version check as the file it replays. A migration backup keeps the original bytes, even when they are not valid UTF-8.

## 6. Connected proof sequence and current evidence discipline

Continue the active coordination program and prerequisite reviews. The following requirements remain, with status determined by current source and fresh verification rather than the older PB/GLM/website handoff wording:

A. The Workbook is retired (PR #56; retired keys refused, #66) and the Console is the one surface, with Conversation and Architect views (#59). Close the gaps the removal exposed: Keep/Undo for waiting changes (P06 adds Keep and Undo on a task thread's Review changes card, PR #106), Make tasks from this plan, the Fix-attempt chip, and a conversation Stop in the Console. Guided/Standard/Technical changes density, not authority; selected appearance remains intact. Every exit from the Console document editor now passes one unsaved-writing gate, and the editor takes no typing until a file's kind is settled (DIO-85, DIO-87, PR #84). CD-05's 200 percent zoom (C47, at 1440 x 900 and the 800 x 600 minimum) and reduced motion (C49) have executed browser evidence (DIO-76, PR #94, on main through #106); CD-05 stays open for the populated results ledger, the packaged AWS journey and a live Luna call and spend.
B. Preserve scoped grants, exact consequential approvals, separate reviewer routing, truthful attribution, policy-before-effects, bounded correction, evidence and recovery. An automatic reviewer can withhold, never widen authority; a remembered approval the person accepted is an exact approval of a bounded pattern and satisfies this rule (D5); Full access requires a proved eligible environment. Remembered approvals (D5) are implemented in source for harness step approvals (PR #83, on main through #86, reviewed with fixes in PR #91) and for Codex direct text proposals, where a Codex task scope is also now bound to the ChatGPT account it was confirmed under (PR #99, on main through #106). Review E accepted the Codex half with fixes (PR #113, in batch 6). A scope confirmed after a declined proposal on another account no longer covers that account. Saved evidence re-pointed at another file's grant no longer validates. The approved-then-switched case and the always-ask list are QUESTIONS.md O42. Both routes have the learned offer and its remembered answer, the always-asks classifier, truthful attribution and one revocation place. The automation's Rules and access view is a follow-up.
C. Prove supported engine discovery/authentication, real route capabilities and first-run setup without hidden paid prompts, copied credentials or billing changes.
D. Extend local setup through trusted model/runtime installation, compatibility checks, profiles, benchmarking, lifecycle/resource ownership and honest health.
E. Prove a real Diomedes-led workflow with typed actions, useful correction, scoped authority, interruption/restart and postcondition verification; prove a second route/context without replacing durable semantics. H13, H12, H15 and H17 now supply those mechanisms in source, and review D accepted all four with fixes; the proof against a live route is still open.
F. Before shared hosted access, prove production identity/membership, server-checked entitlement, company-held keys, tenant-bound reserve/settle metering, protected distribution, support/data handling and offboarding.
G. Deliver Automations A then B with the existing prerequisites; measure non-coding usefulness, review/correction costs and repeatability. More complex connectors, remote and multi-location execution follow their own gates. A and B are in source (section 2); their packaged journeys and the usefulness measurement are open.
H. Owner Surface (proposed 2026-09-25; docs/product/ADR-owner-surface-2026-09-25.md rev 5; discovery docs/implementation/owner-surface-discovery-2026-09-25.md). Aaron's whiteboard and job-priority request widen MI-DEMO from inventory to the business on a phone. Discovery proposes lanes OS01–OS20 (Linear DIO-108). Task priority (OS01) comes first: a Console-only, local change with package nodes 23c/24c, Linear DIO-110, and decisions in QUESTIONS.md R14. People assignments (OS02) are next. Hosted access waits for MI01 and B01–B03; automation capacity and preemption are OS12. Nothing in this item is built yet, and the other OS lanes are proposals, not package nodes.

The preserved repository body contains distinct implementation evidence for PB-01/PB-03, permissions/Agents, Files/instruction delivery, release journeys and Automatic Change Review/H22. It is not discarded and must not be relabeled 'not started' because the older cloud text omitted it. Focused records include docs/implementation/2026-09-10-reviewer-and-agents.md, docs/implementation/2026-09-12-integration.md, docs/implementation/2026-09-12-har01-delivery.md, docs/implementation/2026-09-13-fil02-export-import.md and docs/implementation/2026-09-15-automatic-change-review.md.

Fixes and reviews landed on 2026-09-24: stale Discovery evidence stays readable, correctable and retirable, an engine binding names the bytes on disk, and declared byte limits count UTF-8 bytes (DIO-84, DIO-86, DIO-88, PR #79); every inventory read and command needs this launch's desktop session token, the local person's membership and the owning organization's access-profile permission, over loopback only (DIO-95, PR #76, main a08a694) — local-principal authorization, not device identity, so MI01 (DIO-62) stays open and inventory stays unmounted in the Console; main's recurring Windows and macOS timing failures were made deterministic, with one product fix to change-review ledger labels (batch 1); PRs #36 and #27 were reviewed after the fact with fixes, and the contract-revision marker now reads accepted, matching the C00.R rollout record (DIO-96, DIO-72, PR #74, main de86916). R27-3, a history read authorized as a status check in `server/inventory/stock-service.ts`, is still open.

For release/build status, read current repository/release metadata, docs/harness/RUNTIME_VERIFICATION.md, docs/harness/CHANGES.md and the exact candidate record under evidence/release-candidates. The September 18 source snapshot includes evidence/release-candidates/diomedes-0.1.3-windows-experimental-20260918-0ecb53df46d6.json. The latest published release is 0.1.11 (candidate diomedes-0.1.11-windows-experimental-20260924-a492c42a5513). Historical v0.1.1 pointers and copied green counts are no longer presented here as the current release or fresh tests. No packaged build carries the 2026-09-24 overnight lanes. No application tests, Windows journey or production availability checks were performed for this documentation update.

## 7. Business and commercial constraints — NC-2026-09-22.1 with the 2026-09-23 tier decisions

Services-assisted software adoption remains the strategy: internal/personal use → consultant operating layer → narrow approved design-partner workflow → measured case study → repeatable hospitality offering → broader reusable business workflows. Sell one recognizable job; retain the universal configurable core. Consulting does not require pretending production SaaS billing is ready.

Commercial reconciliation NC-2026-09-22.1 replaces the retired prices and unresolved-allowance claim. Current website anchors (the site's src/data/pricing.ts): 90-Day Workflow Starter $250/month for three months ($750 total), 500 credits/month, no additional Business charge; Nectovia Business $300/month, 1,000 credits/month; Managed Nectovia Small $750/month, 3,000 credits/month; Standard $1,250/month, 5,000 credits/month; Plus $2,000/month, 8,000 credits/month. Starter retains one workflow/location, up to two supported inputs, 60–90 minutes remote onboarding, human review, its existing exclusions and no automatic continuation. Managed includes Business, not an extra allowance. Fractional AI Ops from $2,000/month is professional time and does not automatically bundle Business or inference. Website service anchors are audit from $2,000, pilot from $4,000, implementation from $9,000 plus $2,500 per additional location, connector builds from $3,000–$5,000 with vendor fees at cost, and Private AI from $15,000 plus hardware. Existing separately scoped service exclusions remain. The owner directs a $99 Solo option for one person with light workloads; its allowance remains unapproved, with 250 credits only a planning proposal. The earlier $500 Business Plus suggestion is not approved.

Credits reflect cumulative eligible model usage, including delegated workers, not fixed task/call counts. Each parent job has a finite credit cap and pre-call reservations. The default caps are approved (Andrew, 2026-09-23): Efficient 20, Focused 50, Thorough 100 credits. Before sending, a job likely to exceed its tier's cap shows a warning that offers to move up a tier or to bypass the cap for that one job; nothing raises a cap silently. No hidden overage, payer switch, retry-budget reset or refund of uncertain spend. Nectovia shows monthly usage percentage, pending/uncertain holds, remaining credits, reset date and separate top-ups; usage is public as request counts and dollar figures stay internal. Customers never pick a route or model; they pick a tier, and the owner maps tiers to routes in AI setup (Andrew, 2026-09-23; shared/tier-map.ts). Efficient is Luna on AWS Bedrock (GPT-5.6 Luna today, GPT-6 Luna once AWS qualifies it). Focused is Gemini 3.8 Flash on Google Vertex AI with a Vertex API key. Thorough is meant for GPT-6 Sol on AWS Bedrock; until that model is qualified no Thorough model is sent and a Thorough request is refused by name. This replaces the September 22 text that made Focused Sol-primary and Thorough Opus 5.5. Routes run on the Vercel AI SDK (AWS Bedrock, Google Vertex, Azure, OpenRouter); every mode can reach every provider; there is no automatic fallback, and Muse remains a possible later fallback. Jev runs through OpenRouter. Web search uses the subscription engines, not a paid search API on model-API routes. Style never changes Ask/Plan/Build authority; Ask and Plan may read the web, project files and connector data, while writes stay behind the guarded proposal flow. Advanced model control is optional for users who request it, while managed inference stays the recommended default. Private data handling is the default; Meta Contributor requires explicit authorized, source-scoped consent and cannot promise ten times more whole jobs.

Eligible Managed plans are intended to include unmetered normal human Luna Ask chat with zero credit debit, not free automation, batches, premium calls, delegated workers or paid research tools. The qualification is a named entitlement, not merely an invoice over $750. Fair-use rate/context/concurrency boundaries must be visible and no hidden monthly message cap may contradict unlimited-chat copy. This benefit, Solo and paid metering remain implementation work, not launched claims.

SDKR's ModelAdapter now carries explicit AWS Bedrock, Google Vertex, Azure and OpenRouter routes (Nectovia routing waves 1 and 2, PRs #38 and #42). Keep Runtime/Trust/RunService, distinct payer and funding evidence, and provider-bound private transcripts. Integrate TypeSafe Jev through the existing evaluation/advisory boundary; it does not issue permissions or execute tools. Preserve the existing WorkOS/Neon control-plane subset; a reported Supabase credit is not migration approval. Test spend on each provider account is limited to $10. Preserve current work claims and acceptance evidence; do not restart accepted B00/H01/CD-1 work.

Existing advisory/audit/pilot/implementation/local-service anchors and founding-pilot exceptions remain in the pricing authority and incorporated detailed bodies. Do not substitute old income scenarios for current quotes. Provider cost, allowance debit and invoice are distinct. Count child/reviewer/correction costs. No automatic overage, hidden payer change, embedded company keys, fake checkout or resale of personal subscriptions. Published Apache-2.0 rights are unchanged.

Use synthetic/approved data initially; prospects are not signed customers or permission. No unapproved names, logos, private projections, fabricated ROI, compliance claims or 24/7 support promise. Measure net time after review/rework/maintenance; salary capacity is not cash savings. Preserve tenant-scoped data/credentials/events/search, backups, restore tests, revocation/offboarding and incident responsibilities. Capital efficiency remains required; no day-one server or uncapped subsidized inference requirement.

Website copy stays plain-language first with optional deeper scope, real build-proven captures, accurate availability and working release links. Service scope disclosures do not prove product automation exists. Preserve the Inspectable Service Scope decision and docs/business/SERVICE_SCOPE_PROGRESSIVE_DISCLOSURE_2026-09-15.md. Website components are not application implementations or operational authority. Historical named website-agent assignments do not supersede current work claims.

## 8. Local Infrastructure and longer-term capabilities

Retain docs/product/2026-09-15-local-infrastructure.md, docs/implementation/2026-09-15-local-infrastructure-work-items.md (LI-01–LI-12) and docs/business/2026-09-15-private-ai.md. Capability/workload → existing routing/admission → provider/runtime adapter → interchangeable hardware. Extend discovery, artifacts/profiles/storage, compatibility, lifecycle, capacity, health, operating windows and scoped remote management without another Runtime, Trust, file authority, scheduler or remote shell. Automations B supplies the shared scheduling boundary; LI operating windows reuse it.

Cloud/local/hybrid are policy-controlled deployment choices. Local failure never authorizes cloud disclosure or payer change. Shared hosts require proved client/location isolation. Always-on, service startup, business-hours and wake/reconnect policies require platform evidence and imply neither HA nor staffed support. Hardware is customer-owned or passed through at cost, without markup; ending Managed support must not intentionally disable purchased infrastructure. Provide a functional self-managed handover where technology/licensing permits. Future TCO assessments may recommend cloud; future Verified Local claims need reproducible exact-configuration evidence.

Durable deterministic-plus-agent workflows, waits/queues and remote device identity/pairing/revocation remain requirements under the shared Runtime. Broader orchestration, secure phone status/approval/stop, replay/forks, execution portability/isolation, artifacts without forced GitHub, teach-by-demonstration, MCP Apps/A2A, governed memory/skills and evidence-based improvement remain longer-term extensions. Basic Automations visibility and the bounded recurring slice are no longer buried only in a generic long-term list.

Learning proceeds through observed evidence → candidate rules/skills/prompts/routes → replay/evaluation → reviewed versioned promotion → monitoring/rollback. Customer facts prefer governed retrieval/configuration; weight tuning requires legitimate data, provenance, held-out evaluation and measurable benefit. Learning cannot expand permissions, secrets, payer/data policy or tenant scope.

## 9. Open decisions and publication boundary

Existing open decisions remain: production identity/recovery and device keys; billing/offline entitlement; signed distribution; isolation eligibility; supported model/runtime matrix; client data isolation; detailed workflow representation and scheduling defaults; first authorized external effect; named connectors; plan/host/location limits; support coverage; hiring triggers; native supervisor specialization. The hosted allowance is no longer open: the owner set it on 2026-09-19 (pricing authority, "Included AI usage"); what remains is building the request counter, the per-request ceiling and a company-held provider route. The Automations section and shared-runtime direction are now planned explicitly; detailed contracts, implementation reviews and evidence still gate construction and availability. The 2026-09-24 lanes add open decisions, each running on a recorded conservative default until Andrew answers: Automations A and B defaults (DST gap, catch-up window, who may pause); Ready queue consent and limits; remembered-approval threshold and pattern; control confirmation and uncertain-effect acknowledgement; kept native threads and sessions; OpenCode as a Console conversation route; Agent-profile fallback; effect reconciliation; project commands as verification checks; inventory mapping and mounting; the R36-4 status-line wording; where a loop starts; guidance-revision signing and thresholds; H14 worker limits, budgets and failure; P04 contribution loading; Claude Code project threads on the native session; Cursor and Devin as Console routes; the Codex Resume wait under the service lock; a new start while a supervision escalation is open; and review E's D5, Automations B and P06 items. QUESTIONS.md O11–O42 list those still open. On 2026-09-24 Andrew answered the P06 placement and the H13 and H15 defaults, and set the team-mode rules (QUESTIONS.md R10–R13).

PILLAR IMPACT: advances P01, P02 and P04–P12; no intentional pillar supersession. The 2026-09-24 and 2026-09-25 checkpoints record decisions already taken by Andrew and work landed in source. No lane record asked for a pillar change, and none is made. Andrew's 2026-09-24 decisions refine standing decision 13's placement of readable diffs and set team-mode rules that apply decisions 7 and 8 to helpers.
ROADMAP IMPACT: 2026-09-24.1 reconciles the diverged cloud and repository copies; records the tier map, approved job caps, the Workbook/History/Sample removals, the Conversation and Architect views, releases 0.1.8–0.1.11 and the Automations Milestone A decisions. 2026-09-24.2 records the overnight lanes: Automations A and B, H02, H04, H07, H08, H09, H11, H12, H13, H15, H17, H18, H21, P01/P03, P05, P06, remembered approvals, CD-05 evidence and the audit, inventory, editor and CI fixes, each implemented in source with its review status, and none live, hosted, packaged or device-proven. 2026-09-25.1 applies Andrew's 2026-09-24 decisions: P06 diffs are Core, and H13's finish, sandboxed delegation and limits, H15's supervision defaults and the team-mode rules are settled (QUESTIONS.md R10–R13), with the delegate sandbox not yet landed. It also records batch 4 (PR #106, 8d20fb9) and batch 5 (PR #107, 4395331, H03 and H05) as merged, and batch 6 (PR #119) as landing with H10, H14, P04 and reviews C, D and E. Every feature those reviews covered was accepted with fixes. Review F of H03 and H05 is running, and H16, P07, H20, DIO-107 and the delegate sandbox are still in progress. 2026-09-25.2 adds the Owner Surface program (section 6 H): ADR rev 5, the OS-DISC-01 discovery report, the OS01 task-priority build prompt rev 2 with package nodes 23c/24c, and the priority decisions in QUESTIONS.md R14. No code.
BUILD STATUS: unchanged by this documentation work; no application tests run for it. The lanes' own gate counts are in their records.
PUBLICATION STATUS: the repository mirror is at 2026-09-25.2; the cloud canonical still carries 2026-09-24.1, and cloud synchronisation of the 2026-09-24.2, 2026-09-25.1 and 2026-09-25.2 patches is pending.
DEPLOYMENT STATUS: no app release, website deployment or live customer automation performed by this update. The latest release remains 0.1.11.
