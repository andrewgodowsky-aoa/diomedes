# Package Completion Scope Review: CD-00, CD-01, CD-02, CD-05

Date: 2026-09-21. Scope: the four implementation/review prompt pairs named in the work order, audited against the isolated worktree `F:/Diomedes/diomedes-wt/core-agent-bot` only. The main checkout was not inspected.

Condition assumed throughout: the exact accepted PR29 implementation merges to main. The accepted local candidate is `9060128ad4f42895c045a7a56770ce021a2ada8a`; the pushed PR29 head is `dbaea43b9c1fcd49b37fbb4ad0c85a4f1910b557` with hosted checks running at audit time. The merge has NOT occurred. Under the ledger's DONE rule (`docs/implementation/2026-09-20-core-agent-work-items.md`, opening note), a work order is DONE only when its work is complete, its required tests and independent review passed, and the exact accepted patch is merged to main. Nothing here marks any prompt DONE. This audit concerns bounded pair completion only; it does not certify the wider CD-1 program, a release, a package or a live provider.

## Verdict table

| Pair | Conditional decision after the exact accepted implementation merges | Exact outstanding requirement | Document citations |
| --- | --- | --- | --- |
| CD-00 (03d / 04d) | Does not qualify | No independent CD-00.R verdict exists at all; canonical amendments remain proposed-not-applied pending owner reconciliation with cloud revisions and mirrors | `03d`/`04d` prompt headers ("No independent verdict exists"); `docs/product/2026-09-20-core-diomedes-agent.md` section 11; `docs/implementation/2026-09-21-routing-decision-and-owner-work-order.md` "Exact proposed canonical amendments"; work-items ledger Rounds 12-13, 20 |
| CD-01 (05n / 06n) | Qualifies conditionally on merge, for the accepted seam scope | (a) contract-revision marker conflict open: `shared/contract-revision.ts:39` still `status: 'proposed'` vs the accepted rollout record, unreconciled; (b) bounded acceptance limits retained: no crash-atomic multi-file admission, no process-kill proof, no live provider; (c) C24 tenant authorization recorded absent on the local single-owner path, a scoping rather than an executed proof | `shared/contract-revision.ts`; `docs/implementation/2026-09-21-core-agent-admission-review-successor.md` (`7f1b9b3`); `docs/implementation/2026-09-21-core-agent-composed-review-baseline.md` (`2b3f933`); `docs/implementation/2026-09-21-core-agent-home-composed-review-r2.md` (`9060128`); `docs/implementation/2026-09-20-core-agent-contract.md` (Astra r7 acceptance of `4e2c93c`, Round 8); source map sections 4 and 7 |
| CD-02 (07e / 08e) | Does not qualify | No independent verdict exists for CD-02 as its own lane; its named acceptance set has no dedicated executed review: C21-C23 proposal semantics, no visible task/dispatch/monitoring after a greeting, acknowledgement or brainstorm, refusal-with-reason for a proposal missing objective, target, grant, route or budget, and positive-plus-negative proof rather than keyword rules | `07e`/`08e` prompt headers ("No independent verdict exists for CD-02 as its own lane"); `server/interaction-admission.ts` and `tests/interaction-admission.test.ts` present (producer artifacts, not a verdict); `docs/implementation/cd01-bounded-review-20260921/` scope is CD-01 boundaries; work-items ledger Rounds 2, 9, 16, 19 |
| CD-05 (09b / 10b) | Qualifies conditionally on merge, for the accepted scope | (a) unclaimed: populated results ledger, streaming preview, founder packaged AWS journey, live Luna provider call and spend; (b) no executed evidence recorded for the C47 200-percent-zoom leg or C49 reduced-motion states; (c) disclosed survivors remain limits: client C9 and C21, two inserted-yield driver survivors, plus guarded-not-fixed sentinel CD05-R-04 | `docs/implementation/2026-09-21-core-agent-client-review.md` (R-1 acceptance with named fixes); `2026-09-21-core-agent-client-review-r6.md` (`924066b`, "the previously reviewed CD-05b scope is accepted"); `2026-09-21-core-agent-home-luna-contract-review-r4.md` (`4643f71`); `2026-09-21-core-agent-home-drivers-review-r2.md` (`e2411ca`); `2026-09-21-core-agent-home-server-review-r2.md` (`f37f29a`); `2026-09-21-core-agent-home-client-review-r3.md` (`21879ed`); `2026-09-21-core-agent-home-composed-review-r2.md` (`9060128`) |

## Requirement-to-evidence mapping for the conditional decisions

Every positive clause below is mapped to an independent review document or a reviewed record, not to a producer report.

### CD-01

- One frozen additive contract and source map bound to the inspected interfaces: `docs/implementation/2026-09-20-core-agent-contract.md` (Rounds 3-11) and `2026-09-20-core-agent-source-map.md`. Astra's r7 review accepted candidate `4e2c93c` and ruled that CD-02 and CD-05 may build against the seam as frozen (contract document Round 8; ledger Round 5).
- Six pre-consumer settlements: `auto` Mode merged `265a33c` (ledger Round 2; Ask/Plan narrowing later proved in the executed matrix); thread-to-work identity binding via minted source identity and append-only phases (contract I-21, `shared/conversation-turn-id.ts`); workspace-level conversation via reserved home Project merged `7a6d107` (ledger Round 2); locked admission without Store-lock deadlock proved by the executing successor review's lock trace (`admission-review-successor.md`, soft-deletion and order sections); non-Claude route settled by the accepted Home Luna contract and implementation reviews (`home-luna-contract-review-r4`, `home-server-review-r2`, `home-client-review-r3`); decision recorded as run steps (`interaction-service` phases as transform steps, ledger Round 2).
- C01-C12 expected handling and C24-C30 boundaries: the reviewer's 13-case authority matrix and 13-case seam oracle executed on `7f1b9b3` (successor review), and the five-oracle composed run passed 80/80 on `2b3f933` (composed baseline); the final candidate `9060128` passed the full composed gates (r2 review: TypeScript exit 0, production build exit 0, 176 Playwright, 4,682 Vitest with 4 skipped, 50 page cases).
- One runtime, no second one, fail-closed parsing, existing guards preserved: `server/interaction-service.ts`, `server/interaction-turn.ts`, `server/interaction-admission.ts`, `server/engines/interaction-routes.ts` and `shared/interaction.ts` are present on the integration branch; the successor review confirms the Store -> source queue -> durable child order and records only the two disclosed second-guard survivors M8/M17.
- The "development seam" limit is adopted verbatim: acceptance of CD-01 is acceptance of that seam, not a release or production certification (contract Round 8).

### CD-05

- Page and components: `client/console/Diomedes.tsx`, `client/console/DiomedesHome.tsx` and `client/conversation-send.ts` present. R-1 accepted candidates A and B with named fixes; all four fixes landed (`d157214`, `9973109`, ledger Rounds 5-7).
- Launch and conversation client: R-6 executing acceptance at `924066b` ("the previously reviewed CD-05b scope is accepted"), including the repaired R-12 closures and two intended-red mutation kills.
- Home Luna default and Stop addition (Andrew's PR29 inclusion): contract accepted as bounded design (`home-luna-contract-review-r4.md` at `4643f71`); driver methods accepted (`home-drivers-review-r2.md` at `e2411ca`); server accepted (`home-server-review-r2.md` at `f37f29a`, four replacement mutations red/green); client accepted (`home-client-review-r3.md` at `21879ed`, survivors C9/C21 disclosed).
- Final composed gates: r2 review on `9060128` records all gates green and verifies the composition's application, shared, test and Playwright files match the accepted Home candidate exactly.
- Named acceptance cases: C01 no-task greeting path covered by the home `createTask` invariant (contract I-18) plus executed home-conversation cases; C13 scope switch covered by the R-07 rebinding repair and visit fences (R-5/R-6 reviews); C21 retry admits once via the minted, persisted command id (contract I-22, client r3); C38 progress derived from records with no fabricated percentage, though a populated ledger is unclaimed; C46 Stop is a local control not gated on entitlement and is covered by the driver/server/client acceptances; C47 narrow-viewport leg covered by the CD05-R-03 repair and page cases at 800px, long labels by truncation behavior; C48 keyboard journey covered by the CD05-R-01 repair verified in the running app and the R-12 dialog cases; C50 has no artwork in scope to violate. The C47 200-percent-zoom leg and C49 reduced-motion have no executed evidence in the reviewed record.
- Moved-not-deleted launch pins: the full Playwright suite ran unfiltered to 176 passes on `9060128`, which includes the formerly moved spec files.

### CD-00 and CD-02

- CD-00 work produced the decision record (`docs/product/2026-09-20-core-diomedes-agent.md`) and the ledger; package registration checks pass (ledger Rounds 12-13: 79 items, 158 prompts); the superseded bot program is recorded in `planning/AMENDMENTS.md`; the routing decision resolved the model-default question. None of this substitutes for the missing independent CD-00.R verdict, and the canonical amendments clause of the required acceptance is unmet while the amendments stay proposed.
- CD-02 implementation exists inside the merged branch (`server/interaction-admission.ts` with its 12-case test, admission repair `7f1b9b3`, routes and host port). CD-01-scoped executing reviews exercised mode narrowing, digest binding, interruption and replay, which overlaps but does not constitute the dedicated CD-02 verdict its review prompt requires; both CD-02 prompt files record that no such verdict exists.

## Broader requirements that remain unfulfilled

These are program-level and are not charged against any single pair.

1. PR29 merge itself: hosted checks on `dbaea43` and a drift check must pass before the candidate is PR-ready (r2 review). No merge is recorded.
2. Canonical amendments for Core Pillars, Live Roadmap and Project Memory: proposed, pending owner reconciliation with fresh cloud revisions and repository mirrors (routing document, "Exact proposed canonical amendments"; decision record section 11).
3. Contract-revision marker conflict: `shared/contract-revision.ts` reads `proposed` while the rollout record says `accepted`; the program consumes nothing from that marker until reconciled (both CD-01 prompt headers; source map section 7).
4. Missing independent verdicts: CD-00.R entirely, and CD-02 as its own lane.
5. Unclaimed capabilities: live Luna call, credentials and spend authority (Andrew plus the AWS owner), streaming preview, populated results ledger, crash-atomic admission, founder packaged AWS journey (routing order item 5).
6. Release endgame after merge: version decision and bump, fresh dependency install, release proofs, installer/runtime proofs, publication and site update (routing document; ledger Round 20). The website work also owns the C50 real-screenshot provenance requirement.
7. Later routing milestones: account-aware catalog and Codex discovery (item 6), Muse direct API candidate (item 7), managed customer launch (item 8).
8. CD-04 bounded workers remain unclaimed; the CD-05 availability note requires a CD-04 recipient for real work proof on the page.
9. Disclosed survivors retained as limits, not kills: admission M8/M17 (a second guard refuses the same request), two inserted-yield driver survivors, client survivors C9 and C21, the equivalent route-option survivor, and sentinel CD05-R-04 guarded rather than fixed.
10. Retained schedule/fixture limits: the two animation-frame flushes are observation heuristics, not proofs of every scheduling order; the lock-entry abort fixture models the grant-to-callback boundary, not a real browser Web Lock schedule; the fence orders one process only.

## Evidence limits

- This audit performed no execution, no shell and no Git operations. All test counts and mutation results are quoted from the cited independent review documents and the ledger, and were not re-verified here.
- Byte equality between pushed head `dbaea43` and accepted candidate `9060128` could not be verified. The conditional verdicts assume the merged tree carries the product and test content that the r2 composed review diff-verified against the accepted Home candidate; any code drift between them invalidates the conditional.
- Producer-reported counts were not used for any positive conclusion; only independent verdict documents were.
- Claims of absent evidence (CD-00.R, dedicated CD-02 verdict, C47 zoom leg, C49 reduced motion, populated ledger) are scoped to the documents listed above. If a verdict document outside this set covers any of them, the parent should record it before closing the corresponding gap.
- No prompt file was edited, no status was changed, no file was moved, nothing was marked DONE, and the canonical documents were not touched.
