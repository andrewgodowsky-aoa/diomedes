# Delivery sequence and work ownership

PB-2026-09-10.1 | Immediate priority with bounded implementation slices.

## Current evidence boundary

Published main inspected at a70ae5d9523d81bf5db98304a657cf16ed014a7a; roadmap and memory at 2026-09-10.3 before this package. The running Opus work may be newer and unpublished. These documents define intent, not proof that either Opus pass finished. Inspect actual current source, handoffs and worktree ownership before implementation; do not reset to this recorded hash.

Published shared/types.ts includes legacy onboarding preferences and local-prototype receipts. Shared/harness.ts contains the current principal/tenant/revocation and enforcement vocabulary. Reuse these seams after reconciling the local changes; do not infer production identity or billing from them.

## Opus work before GLM

PB-01 — Identity and workspace entry. Deliver explicit Personal versus Business workspaces, organization/membership/configuration boundaries, safe legacy migration and a Business-only resumable questionnaire. No broad identity-provider rewrite if a current one exists. An absent production provider keeps hosted Business unavailable, but does not prevent a clearly labeled local development proof.

PB-02 — Configuration and harness. Compile answers into a versioned candidate using one supported template. Validate, review, stage and activate through existing services. Instantiate scoped Agents/rules and integrate the existing Team foundation. Demonstrate a source-referenced approved-files draft, not a universal business automation system. Preserve a second synthetic industry variant to show the same primitives are reused.

PB-03 — Managed usage contract. Implement plan/entitlement checks, payer resolution and durable reserve/settle accounting with an available approved backend or an explicitly synthetic service boundary. Keep the $300/$100 example inactive. No live charges, production credentials, new pricing commitments or provider purchases. This gives GLM the budget boundary its retry loop must honor.

PB-04 — Integration handoff. Connect the implemented slices into the actual Console flow, resolve integration defects and document supported/unsupported behavior. Preserve current typography, appearance and existing runtime tests. Hand GLM exact contracts and the new source identity. Do not spend this run implementing GLM's correction loop or a new orchestrator.

Feed the matching short prompts sequentially to the current Opus thread, after its coherent Trust/Agent checkpoint. It may advance through ready dependent work, but unavailable external credentials do not justify fake completion or unbounded side projects. One checkpoint per meaningful slice; not repeated blanket verification or forced subagent review.

## GLM next

Run the previously prepared corrective-execution prompt with prompts/GLM_BUSINESS_ADDENDUM.md. It remains one bounded direct-work route. It now consumes organization, activated configuration, Agent/Team, policy and payer/usage contracts. A correction cannot change tenant, widen authority, evade parent cost limits or reuse approval for changed bytes. Update the handoff from actual Opus source rather than assuming every target shipped.

## Following priorities

After the correction proof: a consented real-model Business onboarding/job trial; one genuinely supported live connection or approved-export workflow; production identity/gateway/billing readiness; safe local-model setup; monitored scheduled work with an actual available host; governed pack updates; then broader configuration automation.

Keep local models, native Diomedes supervision, source-aware context, Needs you, phone status/approval/stop, learning proposals and advanced Teams on the roadmap. They are not all prerequisites for this pre-GLM integration. Model training remains a later measured program, not a dependency for selling a useful managed harness.

## Publication boundaries

This package authorizes and records documentation changes. Implementation follows AGENTS.md and current explicit owner authorization. Do not publish application code, replace the installed executable, deploy the website, change licensing, collect payments or activate production services merely because a prompt says build. A future worker records source/tests/build separately from commit/publication/deployment. Preserve other workers' worktrees and concurrent canonical edits.
