# Proposed integrated source commit - not made

Message: `feat: integrate scoped connections and verify Windows 0.1.1`

Branch: `codex/windows-release-20260909`.
Base: `11829e1962b448360d3fc7aaba7c38fda6833a84`.
Application build-input digest:
`09e551958dfab590f12dc4678664a32c9244f88ef1982723d8fe97b35b292803`.

The explicit proposed source/test/script/doc/license file list is COMMIT_FILES.txt.
It includes the preserved Runtime continuation and Opus Trust import; preserve
their attribution/history when deciding how to integrate. Trust source/tests remain
verbatim. No commit, amend, staging operation or push was performed by this task.

The final change adds scoped synthetic Connections to both desktop surfaces,
generic reviewed OpenAPI fixture adapters and official MCP projection, frozen
context/correction/replay rules, signed event recovery, packaged build identity,
notices, installer/ZIP construction and reproducible proof drivers. It also fixes
prepared-tool dispatch and transcript replay without creating competing Runtime,
Trust, approval or Store authorities.

Validation: 615 unit tests, TypeScript, built client, actual desktop business and
negative cases, source-unavailable relocation, one real Sol synthetic report,
all-file ZIP integrity, actual profile version upgrade, installed-app launch,
repair/uninstall preservation and final package/source hashes. See VERIFICATION.md.

The file list deliberately excludes raw provider proof data/projects, diagnostic
profiles, runtime/account files, build outputs and binary deliverables. Do not use
`git add .`. The local evidence tree includes both historical failed attempts and
final proof. Choose sanitized evidence files explicitly if adding evidence to the
commit; this proposal does not authorize publishing private logs or a repository.

The old runtime-only commit proposal remains historical. This document describes
the integrated final scope. The app, ZIP and installer are unsigned/unpublished;
the complete native dependency notice inventory and actual-download proof remain
open. The .7 roadmap is local with an exact unapplied guarded cloud patch.
