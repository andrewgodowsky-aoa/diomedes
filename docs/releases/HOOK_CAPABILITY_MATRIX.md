# Hook and authority coverage in 0.1.1

These labels describe actual control surfaces, not model intentions. All Connections
credentials, principals and vendor messages in this candidate are explicitly synthetic.

| Surface | Enforced | Observed / instructional | Unsupported or partial |
| --- | --- | --- | --- |
| Context assembly | Trusted host selection by conjunctive tenant/project/connection/capability/role; bounded plain JSON; capability and tool descriptors can only narrow | Rule IDs/versions, source semantics and investigator guidance recorded | General corpus retrieval and arbitrary user code hooks |
| Model dispatch | Mandatory Runtime policy before optional hooks; final effective request frozen in a durable context step; distinct attempt identity/budget; stale-context validation before call | Provider output/transcript reference persisted without rewriting observations | Arbitrary provider-internal data access; OS containment |
| Final output | Bounded inspector response schema and correction limit; corrective model call uses a new Runtime step/budget | Regex/structured match, raw bad answer, corrective instruction and next answer, selected-rule provenance | Stream-time interception, hidden reasoning access, universal semantic correctness |
| Proposed tool | Capability membership, prepared descriptor membership, exact current registry descriptor, schema, effect/destination, current principal and budgets | Typed invocation/result evidence | External agent's private internal tools |
| Connection operation | Current Trust reference and generations, exact instance/tenant/project/resource/operation scope at admission/dispatch/acceptance | Reviewed manifest/source digest and typed normalized result | Live vendor authentication, egress transport and production credential brokerage |
| Raw signed ingress | Raw body limit/schema, exact HMAC/timestamp/location, current connection authority, durable inbox before acknowledgment | Source/received time, receipt digest, conflicts, stale/superseded state | Public endpoint, offline cloud buffering, vendor retry/rate-limit implementation |
| Triage transition | Numeric predicate AND IANA service window on source time; current create-issue authority; one Store transaction for Task/history/receipt | Rule version, source time, reception time, reason and internal Task | Continuous monitoring while host sleeps; external messages or POS writes |
| Replay/recovery | No saved-principal resurrection; fresh explicit authority before processing; old run cancellation, new authority-bound recovery run; no duplicate Task | Prior run IDs and durable observations retained | Automatic authenticated-session restoration and general vendor-side effect reconciliation |
| Improvement adoption | Exact proposal/base/expiry, full scope/type/action/predicate unchanged, bounded prose revision, no enabled-state resurrection | Offline instruction coverage and deterministic replay cases; approved version/rollback | Live-model quality improvement, autonomous privilege expansion, model training |
| Need / recorded write | Existing exact action/base/proposal/expiry, durable decision replay and Store write authority | Preview in both surfaces, execution receipt, History and restore | General external side effects, edits with no exact supported writer |
| MCP | Official SDK projection over the same reviewed invocation and Runtime path; current authorization rechecked after discovery | Client/server in-memory discovery/call and request identity | Standalone network MCP server, remote auth, MCP Apps containment |
| Direct Codex report | Existing frozen initial egress grant, current result-acceptance authority, exact Need and supported local recorded write | Actual native Sol output/usage/opaque transcript; best-effort stop and provider policy declarations | NativeAgent output hooks on external internal streams/tools; reliable provider cancellation after ambiguous dispatch |

Fresh packaged negative proof includes registered write denial, reordered delivery,
strict privilege-request refusal, changed-base protection and valid-state approval
expiry. The expired state was deliberately aged in an isolated stopped profile;
no system-clock change was made. See PACKAGE_NEGATIVE_PROOF.md.

In-process compiler fixtures and hooks are trusted application code. Reviewed
metadata, a worktree, a signature check and a provider policy declaration each prove
different things; none alone establishes OS/network containment or human identity.

ROADMAP IMPACT: this is a bounded Core/Runtime/Trust/Interop proof. It preserves
separate responsibility boundaries and does not authorize production data or claims.
