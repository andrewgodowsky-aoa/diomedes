# Route acceptance matrix — 2026-09-25

Generated by `npm run eval:matrix` (H20). Every cell is derived from the route contracts and the headless runner’s results; nothing below was written by hand.

Environment: linux-x64, Node 22.22.2, commit f5259bbed967.

## Counts

| State | Cells |
|---|---|
| proven here | 43 |
| declared, not proven here | 96 |
| unsupported (declared) | 74 |
| mismatch | 3 |

## Matrix

| Route | turn | stream | tool-proposal | approval | steer | queue | stop | resume | retry | fork | verification | context-accounting |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `claude-code-session` | proven | declared | unsupported | unsupported | proven | proven | proven | proven | declared | proven | unsupported | unsupported |
| `opencode-session` | proven | proven | unsupported | unsupported | proven | proven | **MISMATCH** | proven | declared | proven | unsupported | unsupported |
| `cursor-session` | proven | proven | unsupported | proven | unsupported | proven | **MISMATCH** | **MISMATCH** | declared | unsupported | unsupported | unsupported |
| `devin-session` | declared | declared | unsupported | declared | unsupported | declared | declared | declared | declared | unsupported | unsupported | unsupported |
| `native-fixture` | proven | unsupported | proven | proven | unsupported | declared | unsupported | proven | declared | declared | proven | proven |
| `codex-report` | declared | unsupported | declared | declared | unsupported | declared | unsupported | declared | declared | declared | declared | declared |
| `harness-runtime` | declared | unsupported | declared | declared | unsupported | declared | unsupported | declared | declared | declared | declared | declared |
| `sample` | proven | unsupported | proven | proven | unsupported | unsupported | unsupported | unsupported | unsupported | unsupported | proven | unsupported |
| `codex` | proven | unsupported | proven | proven | unsupported | unsupported | proven | unsupported | proven | unsupported | proven | unsupported |
| `claude-code` | declared | declared | declared | declared | unsupported | unsupported | declared | unsupported | declared | unsupported | declared | unsupported |
| `opencode` | declared | declared | declared | declared | unsupported | unsupported | declared | unsupported | declared | unsupported | declared | unsupported |
| `oh-my-pi` | declared | declared | declared | declared | unsupported | unsupported | declared | unsupported | declared | unsupported | declared | unsupported |
| `cursor` | proven | proven | declared | declared | unsupported | unsupported | declared | unsupported | declared | unsupported | declared | unsupported |
| `devin` | declared | declared | declared | declared | unsupported | unsupported | declared | unsupported | declared | unsupported | declared | unsupported |
| `aws-bedrock` | proven | proven | proven | proven | unsupported | proven | proven | proven | declared | unsupported | proven | proven |
| `azure-openai` | declared | declared | declared | declared | unsupported | declared | declared | declared | declared | unsupported | declared | declared |
| `openrouter` | declared | declared | declared | declared | unsupported | declared | declared | declared | declared | unsupported | declared | declared |
| `google-vertex` | declared | declared | declared | declared | unsupported | declared | declared | declared | declared | unsupported | declared | declared |

## Mismatches

- `opencode-session` · stop: Declared native, but Stop while the session reads busy and not yet connected, at 46 ms: 409 (SESSION_CLOSED: The native process is already closed.); the turn ended at 5496 ms, 409 TIMEOUT (“OpenCode did not finish within the time limit. Recheck before starting another request.”). (scenario opencode-kept-session)
- `cursor-session` · stop: Declared native, but Stop as soon as the turn reads busy: Stop at 41 ms answered 200 (acknowledged true); the turn ended at 10270 ms, 409 TIMEOUT (“Cursor exceeded the request time limit. No retry was sent.”). (scenario cursor-acp-session)
- `cursor-session` · resume: Declared native, but restart while a turn ran, then resume: refused 409 RECONCILE_REQUIRED (“The native turn outcome is uncertain.”); the conversation reads start-again: “Couldn't resume. The last Cursor turn did not finish, so its outcome is unknown. The next message starts a new session.” — though the agent advertises loadSession. (scenario cursor-acp-session)

Scenarios that failed:
- `cursor-acp-session`: its failed checks are the mismatches above.
- `opencode-kept-session`: its failed checks are the mismatches above.

## What the cells not proven here need

- Needs the real claude-code 2.1.252 binary, signed in; no fixture of it drives this capability through Core here. (9): `claude-code-session` stream, `claude-code-session` retry, `claude-code` turn, `claude-code` stream, `claude-code` tool-proposal, `claude-code` approval, `claude-code` stop, `claude-code` retry, `claude-code` verification
- Needs the real opencode 1.18.4 binary, signed in; no fixture of it drives this capability through Core here. (8): `opencode-session` retry, `opencode` turn, `opencode` stream, `opencode` tool-proposal, `opencode` approval, `opencode` stop, `opencode` retry, `opencode` verification
- Needs the real cursor 2026.08.11 binary, signed in; no fixture of it drives this capability through Core here. (6): `cursor-session` retry, `cursor` tool-proposal, `cursor` approval, `cursor` stop, `cursor` retry, `cursor` verification
- Needs the real devin 3000.10.23 binary, signed in; no fixture of it drives this capability through Core here. (14): `devin-session` turn, `devin-session` stream, `devin-session` approval, `devin-session` queue, `devin-session` stop, `devin-session` resume, `devin-session` retry, `devin` turn, `devin` stream, `devin` tool-proposal, `devin` approval, `devin` stop, `devin` retry, `devin` verification
- No scenario in this run exercises it. (12): `native-fixture` queue, `native-fixture` retry, `native-fixture` fork, `harness-runtime` turn, `harness-runtime` tool-proposal, `harness-runtime` approval, `harness-runtime` queue, `harness-runtime` resume, `harness-runtime` retry, `harness-runtime` fork, `harness-runtime` verification, `harness-runtime` context-accounting
- Needs the real codex 0.153.4 binary, signed in; no fixture of it drives this capability through Core here. (9): `codex-report` turn, `codex-report` tool-proposal, `codex-report` approval, `codex-report` queue, `codex-report` resume, `codex-report` retry, `codex-report` fork, `codex-report` verification, `codex-report` context-accounting
- Needs the real oh-my-pi 18.0.6 binary, signed in; no fixture of it drives this capability through Core here. (7): `oh-my-pi` turn, `oh-my-pi` stream, `oh-my-pi` tool-proposal, `oh-my-pi` approval, `oh-my-pi` stop, `oh-my-pi` retry, `oh-my-pi` verification
- Needs a live aws-bedrock credential or a transport fixture that exercises this capability; none ran here. (1): `aws-bedrock` retry
- Needs a live azure-openai credential or a transport fixture that exercises this capability; none ran here. (10): `azure-openai` turn, `azure-openai` stream, `azure-openai` tool-proposal, `azure-openai` approval, `azure-openai` queue, `azure-openai` stop, `azure-openai` resume, `azure-openai` retry, `azure-openai` verification, `azure-openai` context-accounting
- Needs a live openrouter credential or a transport fixture that exercises this capability; none ran here. (10): `openrouter` turn, `openrouter` stream, `openrouter` tool-proposal, `openrouter` approval, `openrouter` queue, `openrouter` stop, `openrouter` resume, `openrouter` retry, `openrouter` verification, `openrouter` context-accounting
- Needs a live google-vertex credential or a transport fixture that exercises this capability; none ran here. (10): `google-vertex` turn, `google-vertex` stream, `google-vertex` tool-proposal, `google-vertex` approval, `google-vertex` queue, `google-vertex` stop, `google-vertex` resume, `google-vertex` retry, `google-vertex` verification, `google-vertex` context-accounting

This run was on linux-x64. No route contract declares a capability as specific to one operating system, so no cell is derived as needing Windows; a platform difference would show here as a mismatch on the platform where it happens.

## Scenarios

| Scenario | Fixture | Outcome | Checks | Assertions |
|---|---|---|---|---|
| Ask and answer on AWS Bedrock: a streamed turn and its context account, a follow-up, Stop mid-turn, a proposal approved and verified, and resume after a restart (`aws-ask-answer-stream`) | aws-bedrock: captured-shape Responses stream (tests/fixtures/model-api-streams.ts) below the AI SDK | passed | 9/9 | 1/1 |
| A proposal needing approval: approve, and decline (`sample-proposal-approve-decline`) | sample: the staged worker inside Core | passed | 4/4 | 0/0 |
| Controls the staged worker declares unsupported are refused, with the contract’s reason (`sample-controls-refused`) | sample: the staged worker inside Core | passed | 5/5 | 3/3 |
| Stop mid-turn on the Codex route (`codex-stop-mid-turn`) | codex: scripted Work call (nativeGenerator) that holds until its signal aborts | passed | 2/2 | 1/1 |
| Retry after failure on the Codex route, then approve and verify the retried run (`codex-retry-after-failure`) | codex: scripted Work call (nativeGenerator) that fails once, then proposes | passed | 5/5 | 3/3 |
| H13 loop: restart while it waits for approval, resume from the record, finish Verified (H17) (`h13-loop-restart-verified`) | native-fixture: the Diomedes loop’s fixed local script (loopFixtureAdapter, delegateFixtureAdapter) | passed | 6/6 | 1/1 |
| H12: a write whose outcome is uncertain after a crash blocks Retry, and nothing is sent (`h12-uncertain-effect-blocks-retry`) | codex: tests/approval-crash-child.ts, a real Core process that exits after the first file of an approved write; then an outside edit | passed | 0/0 | 4/4 |
| H15: a run writing outside its selected folder is paused and escalated to a person (`h15-drift-escalation`) | sample: the staged worker inside Core | passed | 0/0 | 4/4 |
| H17: declared checks verify a finished run against exact bytes; an outside edit makes it uncertain (`h17-verification-evidence`) | sample: the staged worker inside Core | passed | 1/1 | 3/3 |
| A kept Cursor conversation: turns, a follow-up, a plan approved and declined, Stop mid-turn, and resume after a restart (`cursor-acp-session`) | cursor: tests/fixtures/acp-agent.mjs, a real ACP v1 agent process standing in for the Cursor CLI | failed | 10/12 | 1/1 |
| A kept OpenCode session: turns, a message held and sent after the running turn, resume after a restart, fork, and Stop mid-turn (`opencode-kept-session`) | opencode: tests/fixtures/opencode-session-server.mjs, a real HTTP+SSE server standing in for `opencode serve` | failed | 7/8 | 0/0 |
| A Claude Code thread: a turn, a follow-up in the same process, a message queued behind a running answer, Stop mid-turn, and resume after a restart (`claude-code-native-session`) | claude-code: tests/fixtures/claude-stream-json.mjs, a stream-json child standing in for the Claude Code CLI (the H03 route tests’ script) | passed | 6/6 | 0/0 |

- `aws-ask-answer-stream`
  - pass · `aws-bedrock` turn performs: the turn answered 200: “Answered H20-FIRST.”.
  - pass · `aws-bedrock` stream performs: 5 transient text deltas reached the Console’s event stream for this thread.
  - pass · `aws-bedrock` context-accounting performs: H18 account on the answered turn: 7 sections, 1089 estimated tokens, provider reported 900 input tokens.
  - pass · `aws-bedrock` queue performs: a follow-up message was answered (200) as a new bounded turn whose request carried the earlier message (true).
  - pass · `aws-bedrock` stop performs: interrupt answered 200; the turn ended 200 with interrupted=true.
  - pass · `aws-bedrock` tool-proposal performs: the conversation proposed acting (proposed); selected, Work on aws-bedrock proposed Linen checklist.md as an exact Need before the file existed.
  - pass · `aws-bedrock` approval performs: the exact approval was recorded; the file holds the approved bytes (true); the run ended done.
  - pass · `aws-bedrock` verification performs: H17 on the Work run: Verified — 1 declared check passed against 1 exact file version.
  - pass · `aws-bedrock` resume performs: after a restart the next message was answered (200) and its request carried the conversation from the durable record (true).
  - pass · every provider request stayed below the SDK fixture: 6 captured requests
- `sample-proposal-approve-decline`
  - pass · `sample` approval performs: declining “add a file called Sample work notes.md to the project folder” left Sample work notes.md unwritten (exists: false).
  - pass · `sample` turn performs: the staged run started and finished (done).
  - pass · `sample` tool-proposal performs: the run proposed “add a file called Sample work notes.md to the project folder” as a Need before the file existed.
  - pass · `sample` approval performs: after go-ahead the file was written (true) and History recorded it.
- `sample-controls-refused`
  - pass · `sample` steer refuses: steer was refused: A staged run has no steering channel. Queue the message to send it after this turn.
  - pass · `sample` stop refuses: stop was refused: This route cannot stop only the request in flight: Stop is the only interruption.
  - pass · `sample` resume refuses: resume was refused: A stopped staged run restarts from the beginning.
  - pass · `sample` retry refuses: retry was refused: A staged run has no mid-run retry.
  - pass · `sample` fork refuses: fork was refused: A staged run has no lineage.
  - pass · the task-level Stop, which Diomedes composes on every route, applies: stop task: applied
  - pass · Diomedes’ own follow-up queue holds a message on a route whose contract declares no follow-up (it is not the route’s follow-up): queue: queued, performed by diomedes
  - pass · every control, refused or not, left a receipt: 7 receipts
- `codex-stop-mid-turn`
  - pass · `codex` steer refuses: steer on a running Codex turn with no recorded thread: refused (unsupported)
  - pass · `codex` stop performs: Stop (generation) was applied and acknowledged; the in-flight call saw its abort (true); no proposal was written (true); the run is working.
  - pass · the task Stop then ends the interrupted run: stop task applied; run stopped
- `codex-retry-after-failure`
  - pass · `codex` retry performs: retry was applied, attempt 2, as Scc2c3b94c176.
  - pass · `codex` turn performs: the retried turn answered with a proposal for Fall menu.md.
  - pass · `codex` tool-proposal performs: an exact proposal waits on a Need with digests (true).
  - pass · `codex` approval performs: the exact approval was recorded and the file holds the approved bytes (true).
  - pass · `codex` verification performs: H17 on the retried run: Verified — 2 declared checks passed against 1 exact file version.
  - pass · the first attempt failed: failed
  - pass · the failed original is kept as evidence, still failed: failed
  - pass · the scripted Codex call ran exactly twice: once failed, once retried: 2 calls
- `h13-loop-restart-verified`
  - pass · `native-fixture` turn performs: planned 4 items, took 4 turns, finished with “Proposed Harness report.md from order.md and a helper's reading of delivery.md.”.
  - pass · `native-fixture` tool-proposal performs: the propose_write tool step asked for Harness report.md through a Need.
  - pass · `native-fixture` approval performs: the exact approval, given after the restart, was recorded.
  - pass · `native-fixture` resume performs: after a restart the reopened Core continued the run from its record; the 2 turns observed before it read the same after it (true).
  - pass · `native-fixture` verification performs: H17 at the finish: Verified — 2 declared checks passed against 1 exact file version.
  - pass · `native-fixture` context-accounting performs: H18 account: 7 sections, 950 estimated tokens (utf8-bytes/4).
  - pass · H17 evidence binds the exact bytes it judged: 1 bound file versions
- `h12-uncertain-effect-blocks-retry`
  - pass · the Core process exited at the durable boundary after the first approved file: exit 83
  - pass · recovery kept the outside edit and recorded the write as conflicted, the run as failed: execution conflicted, run failed
  - pass · Retry is refused as uncertain-effects and names the effect: refused (uncertain-effects): Some of the approved change “apply the proposed changes to 2 files” may have been written while outside edits were kept.
  - pass · nothing was sent and no run was started: 0 engine calls, 1 runs
- `h15-drift-escalation`
  - pass · scope drift is detected from the durable records and escalated as an ordinary Need: scope-drift: Diomedes paused this run: it started writing outside the selected folder
  - pass · the run was paused through an H08 Stop asked in supervision’s name and performed by Diomedes: run stopped; stop requested via supervision
  - pass · the escalation names the evidence it rests on: Reopening plan.md
  - pass · re-evaluating raises nothing new for the same issue: 1 escalations after re-evaluating (0 records)
- `h17-verification-evidence`
  - pass · `sample` verification performs: Verified: 1 declared check passed against 4 exact file versions.
  - pass · with no checks declared the run reads Not verified: Not verified: No acceptance checks are declared for this task, so nothing was verified.
  - pass · the verification is a History entry that binds the digest it judged: entry E21fde27ff4b7: verified
  - pass · an outside edit to a verified file turns the result uncertain on the next read: Verification uncertain: Fall menu.md changed after verification, so the result no longer describes these bytes.
- `cursor-acp-session`
  - pass · `cursor-session` turn performs: the first turn answered 200: “answer after 0 earlier turns”.
  - pass · `cursor-session` stream performs: 1 transient text deltas reached the event stream.
  - pass · `cursor-session` queue performs: a follow-up continued the same ACP session: “answer after 1 earlier turns”.
  - pass · `cursor-session` steer refuses: no steer route is mounted for a contract that declares none (404).
  - pass · `cursor-session` fork refuses: fork answered 409: The native session contract does not match this route and build.
  - pass · `cursor-session` approval performs: the plan Cursor presented mid-turn became a Need (“Cursor asks you to approve its plan: Outline the report in three sections”); go-ahead reached the agent: “plan accepted; outlined”.
  - pass · `cursor` turn performs: a direct Ask on the text route answered 200: “answer after 0 earlier turns” — a fresh session, no history.
  - pass · `cursor` stream performs: 1 transient text deltas reached the event stream for the direct Ask.
  - pass · `cursor-session` stop performs: Stop once the prompt is running: Stop at 356 ms answered 200 (acknowledged true); the turn ended at 370 ms, 200 interrupted=true.
  - pass · `cursor-session` resume performs: restart while idle, then resume: the saved ACP session was loaded (“answer after 4 earlier turns”).
  - FAIL · `cursor-session` resume performs: restart while a turn ran, then resume: refused 409 RECONCILE_REQUIRED (“The native turn outcome is uncertain.”); the conversation reads start-again: “Couldn't resume. The last Cursor turn did not finish, so its outcome is unknown. The next message starts a new session.” — though the agent advertises loadSession.
  - FAIL · `cursor-session` stop performs: Stop as soon as the turn reads busy: Stop at 41 ms answered 200 (acknowledged true); the turn ended at 10270 ms, 409 TIMEOUT (“Cursor exceeded the request time limit. No retry was sent.”).
  - pass · a declined plan tells the agent no: “plan rejected”
- `opencode-kept-session`
  - pass · `opencode-session` turn performs: the first turn answered 200: “answer:first (turn 1 of ses_0001096faa)”.
  - pass · `opencode-session` stream performs: 1 transient text deltas reached the event stream.
  - pass · `opencode-session` queue performs: a follow-up went to the same OpenCode session: “answer:second (turn 2 of ses_0001096faa)”.
  - pass · `opencode-session` steer performs: a message sent while a turn ran was pending, then delivered; the thread shows it as its own turn (true).
  - pass · `opencode-session` resume performs: closed, restarted, then resumed the same OpenCode session: “answer:resumed (turn 5 of ses_0001096faa)”.
  - pass · `opencode-session` fork performs: fork started opencode-5bfc091a0b6642b6fbf41f906a2caf08854bf425bc5c03c98c8521237d946ee1 on a new OpenCode session in the same lineage: “answer:forked (turn 6 of ses_000275cfb4)”.
  - pass · `opencode-session` stop performs: Stop while the session reads busy and connected, at 528 ms: 200 (acknowledged true); the turn ended at 537 ms, 200 interrupted=true.
  - FAIL · `opencode-session` stop performs: Stop while the session reads busy and not yet connected, at 46 ms: 409 (SESSION_CLOSED: The native process is already closed.); the turn ended at 5496 ms, 409 TIMEOUT (“OpenCode did not finish within the time limit. Recheck before starting another request.”).
- `claude-code-native-session`
  - pass · `claude-code-session` turn performs: the first turn answered 200: “Answer to Good morning”.
  - pass · `claude-code-session` queue performs: a follow-up was answered by the same live process (1 process): “Answer to Where is the linen?”.
  - pass · `claude-code-session` steer performs: a message sent while an answer ran was held and sent as the next turn of the same process (delivered).
  - pass · `claude-code-session` stop performs: Stop answered 200 (interrupted); the turn ended interrupted=true; the session stayed live.
  - pass · `claude-code-session` resume performs: after a restart the thread read resumable; the next message resumed the session by id (--resume: true).
  - pass · `claude-code-session` fork performs: fork started claude-92343d7016140448b3e18aeda7f2bf079ea4cf4b20d5c58bf9a6d0ab548b1340 with --resume and --fork-session (true): “Answer to Try another way”.

## Every cell

### `claude-code-session`

external-session, engine `claude-code` 2.1.252, authentication native-sign-in, tested with 2.1.252.

| Capability | State | Declared | Why |
|---|---|---|---|
| turn | proven here | native (`commands.start`) | the first turn answered 200: “Answer to Good morning”. Fixture: claude-code: tests/fixtures/claude-stream-json.mjs, a stream-json child standing in for the Claude Code CLI (the H03 route tests’ script). |
| stream | declared, not proven here | native (`streaming.transientPreview`) | Declared native. Needs the real claude-code 2.1.252 binary, signed in; no fixture of it drives this capability through Core here. |
| tool-proposal | unsupported (declared) | unsupported (`routeId`) | Declared unsupported: A kept conversation answers in text; it is not a Work route and declares no proposal path. |
| approval | unsupported (declared) | unsupported (`routeId`) | Declared unsupported: A kept conversation declares no approval path of its own. |
| steer | proven here | host (`commands.steer`) | a message sent while an answer ran was held and sent as the next turn of the same process (delivered). Fixture: claude-code: tests/fixtures/claude-stream-json.mjs, a stream-json child standing in for the Claude Code CLI (the H03 route tests’ script). |
| queue | proven here | native (`commands.follow-up`) | a follow-up was answered by the same live process (1 process): “Answer to Where is the linen?”. Fixture: claude-code: tests/fixtures/claude-stream-json.mjs, a stream-json child standing in for the Claude Code CLI (the H03 route tests’ script). |
| stop | proven here | native (`commands.interrupt`) | Stop answered 200 (interrupted); the turn ended interrupted=true; the session stayed live. Fixture: claude-code: tests/fixtures/claude-stream-json.mjs, a stream-json child standing in for the Claude Code CLI (the H03 route tests’ script). |
| resume | proven here | native (`commands.resume`) | after a restart the thread read resumable; the next message resumed the session by id (--resume: true). Fixture: claude-code: tests/fixtures/claude-stream-json.mjs, a stream-json child standing in for the Claude Code CLI (the H03 route tests’ script). |
| retry | declared, not proven here | host (`commands.retry`) | Declared host. Needs the real claude-code 2.1.252 binary, signed in; no fixture of it drives this capability through Core here. |
| fork | proven here | native (`commands.fork`) | fork started claude-92343d7016140448b3e18aeda7f2bf079ea4cf4b20d5c58bf9a6d0ab548b1340 with --resume and --fork-session (true): “Answer to Try another way”. Fixture: claude-code: tests/fixtures/claude-stream-json.mjs, a stream-json child standing in for the Claude Code CLI (the H03 route tests’ script). |
| verification | unsupported (declared) | unsupported (`routeId`) | Declared unsupported: H17 verifies a finished Work run; a kept conversation is not one. |
| context-accounting | unsupported (declared) | unsupported (`mode`) | Declared unsupported: The engine builds and manages its own context; Diomedes measures none of it. |

### `opencode-session`

external-session, engine `opencode` 1.18.4, authentication native-sign-in, tested with 1.18.4.

| Capability | State | Declared | Why |
|---|---|---|---|
| turn | proven here | native (`commands.start`) | the first turn answered 200: “answer:first (turn 1 of ses_0001096faa)”. Fixture: opencode: tests/fixtures/opencode-session-server.mjs, a real HTTP+SSE server standing in for `opencode serve`. |
| stream | proven here | native (`streaming.transientPreview`) | 1 transient text deltas reached the event stream. Fixture: opencode: tests/fixtures/opencode-session-server.mjs, a real HTTP+SSE server standing in for `opencode serve`. |
| tool-proposal | unsupported (declared) | unsupported (`routeId`) | Declared unsupported: A kept conversation answers in text; it is not a Work route and declares no proposal path. |
| approval | unsupported (declared) | unsupported (`routeId`) | Declared unsupported: A kept conversation declares no approval path of its own. |
| steer | proven here | host (`commands.steer`) | a message sent while a turn ran was pending, then delivered; the thread shows it as its own turn (true). Fixture: opencode: tests/fixtures/opencode-session-server.mjs, a real HTTP+SSE server standing in for `opencode serve`. |
| queue | proven here | native (`commands.follow-up`) | a follow-up went to the same OpenCode session: “answer:second (turn 2 of ses_0001096faa)”. Fixture: opencode: tests/fixtures/opencode-session-server.mjs, a real HTTP+SSE server standing in for `opencode serve`. |
| stop | mismatch | native (`commands.interrupt`) | Declared native, but Stop while the session reads busy and not yet connected, at 46 ms: 409 (SESSION_CLOSED: The native process is already closed.); the turn ended at 5496 ms, 409 TIMEOUT (“OpenCode did not finish within the time limit. Recheck before starting another request.”). Fixture: opencode: tests/fixtures/opencode-session-server.mjs, a real HTTP+SSE server standing in for `opencode serve`. |
| resume | proven here | native (`commands.resume`) | closed, restarted, then resumed the same OpenCode session: “answer:resumed (turn 5 of ses_0001096faa)”. Fixture: opencode: tests/fixtures/opencode-session-server.mjs, a real HTTP+SSE server standing in for `opencode serve`. |
| retry | declared, not proven here | host (`commands.retry`) | Declared host. Needs the real opencode 1.18.4 binary, signed in; no fixture of it drives this capability through Core here. |
| fork | proven here | native (`commands.fork`) | fork started opencode-5bfc091a0b6642b6fbf41f906a2caf08854bf425bc5c03c98c8521237d946ee1 on a new OpenCode session in the same lineage: “answer:forked (turn 6 of ses_000275cfb4)”. Fixture: opencode: tests/fixtures/opencode-session-server.mjs, a real HTTP+SSE server standing in for `opencode serve`. |
| verification | unsupported (declared) | unsupported (`routeId`) | Declared unsupported: H17 verifies a finished Work run; a kept conversation is not one. |
| context-accounting | unsupported (declared) | unsupported (`mode`) | Declared unsupported: The engine builds and manages its own context; Diomedes measures none of it. |

### `cursor-session`

external-session, engine `cursor` 2026.08.11, authentication native-sign-in, tested with 2026.08.11.

| Capability | State | Declared | Why |
|---|---|---|---|
| turn | proven here | native (`commands.start`) | the first turn answered 200: “answer after 0 earlier turns”. Fixture: cursor: tests/fixtures/acp-agent.mjs, a real ACP v1 agent process standing in for the Cursor CLI. |
| stream | proven here | native (`streaming.transientPreview`) | 1 transient text deltas reached the event stream. Fixture: cursor: tests/fixtures/acp-agent.mjs, a real ACP v1 agent process standing in for the Cursor CLI. |
| tool-proposal | unsupported (declared) | unsupported (`routeId`) | Declared unsupported: A kept conversation answers in text; it is not a Work route and declares no proposal path. |
| approval | proven here | native (`engine.protocolVersion`) | the plan Cursor presented mid-turn became a Need (“Cursor asks you to approve its plan: Outline the report in three sections”); go-ahead reached the agent: “plan accepted; outlined”. Fixture: cursor: tests/fixtures/acp-agent.mjs, a real ACP v1 agent process standing in for the Cursor CLI. |
| steer | unsupported (declared) | unsupported (`commands.steer`) | Declared unsupported, and the refusal was observed: no steer route is mounted for a contract that declares none (404). Fixture: cursor: tests/fixtures/acp-agent.mjs, a real ACP v1 agent process standing in for the Cursor CLI. |
| queue | proven here | native (`commands.follow-up`) | a follow-up continued the same ACP session: “answer after 1 earlier turns”. Fixture: cursor: tests/fixtures/acp-agent.mjs, a real ACP v1 agent process standing in for the Cursor CLI. |
| stop | mismatch | native (`commands.interrupt`) | Declared native, but Stop as soon as the turn reads busy: Stop at 41 ms answered 200 (acknowledged true); the turn ended at 10270 ms, 409 TIMEOUT (“Cursor exceeded the request time limit. No retry was sent.”). Fixture: cursor: tests/fixtures/acp-agent.mjs, a real ACP v1 agent process standing in for the Cursor CLI. |
| resume | mismatch | native (`commands.resume`) | Declared native, but restart while a turn ran, then resume: refused 409 RECONCILE_REQUIRED (“The native turn outcome is uncertain.”); the conversation reads start-again: “Couldn't resume. The last Cursor turn did not finish, so its outcome is unknown. The next message starts a new session.” — though the agent advertises loadSession. Fixture: cursor: tests/fixtures/acp-agent.mjs, a real ACP v1 agent process standing in for the Cursor CLI. |
| retry | declared, not proven here | host (`commands.retry`) | Declared host. Needs the real cursor 2026.08.11 binary, signed in; no fixture of it drives this capability through Core here. |
| fork | unsupported (declared) | unsupported (`commands.fork`) | Declared unsupported, and the refusal was observed: fork answered 409: The native session contract does not match this route and build. Fixture: cursor: tests/fixtures/acp-agent.mjs, a real ACP v1 agent process standing in for the Cursor CLI. |
| verification | unsupported (declared) | unsupported (`routeId`) | Declared unsupported: H17 verifies a finished Work run; a kept conversation is not one. |
| context-accounting | unsupported (declared) | unsupported (`mode`) | Declared unsupported: The engine builds and manages its own context; Diomedes measures none of it. |

### `devin-session`

external-session, engine `devin` 3000.10.23, authentication native-sign-in, tested with 3000.10.23.

| Capability | State | Declared | Why |
|---|---|---|---|
| turn | declared, not proven here | native (`commands.start`) | Declared native. Needs the real devin 3000.10.23 binary, signed in; no fixture of it drives this capability through Core here. |
| stream | declared, not proven here | native (`streaming.transientPreview`) | Declared native. Needs the real devin 3000.10.23 binary, signed in; no fixture of it drives this capability through Core here. |
| tool-proposal | unsupported (declared) | unsupported (`routeId`) | Declared unsupported: A kept conversation answers in text; it is not a Work route and declares no proposal path. |
| approval | declared, not proven here | native (`engine.protocolVersion`) | Declared native. Needs the real devin 3000.10.23 binary, signed in; no fixture of it drives this capability through Core here. |
| steer | unsupported (declared) | unsupported (`commands.steer`) | Declared unsupported: ACP has no in-band steering channel; a message waits for the turn to end. |
| queue | declared, not proven here | native (`commands.follow-up`) | Declared native. Needs the real devin 3000.10.23 binary, signed in; no fixture of it drives this capability through Core here. |
| stop | declared, not proven here | native (`commands.interrupt`) | Declared native. Needs the real devin 3000.10.23 binary, signed in; no fixture of it drives this capability through Core here. |
| resume | declared, not proven here | native (`commands.resume`) | Declared native. Needs the real devin 3000.10.23 binary, signed in; no fixture of it drives this capability through Core here. |
| retry | declared, not proven here | host (`commands.retry`) | Declared host. Needs the real devin 3000.10.23 binary, signed in; no fixture of it drives this capability through Core here. |
| fork | unsupported (declared) | unsupported (`commands.fork`) | Declared unsupported: ACP v1 has no fork of a saved session. |
| verification | unsupported (declared) | unsupported (`routeId`) | Declared unsupported: H17 verifies a finished Work run; a kept conversation is not one. |
| context-accounting | unsupported (declared) | unsupported (`mode`) | Declared unsupported: The engine builds and manages its own context; Diomedes measures none of it. |

### `native-fixture`

harness-agent, engine `native-fixture` 1, authentication development-fixture, tested with nothing external.

| Capability | State | Declared | Why |
|---|---|---|---|
| turn | proven here | native (`commands.start`) | planned 4 items, took 4 turns, finished with “Proposed Harness report.md from order.md and a helper's reading of delivery.md.”. Fixture: native-fixture: the Diomedes loop’s fixed local script (loopFixtureAdapter, delegateFixtureAdapter). |
| stream | unsupported (declared) | unsupported (`streaming.transientPreview`) | Declared unsupported: The route declares no transient preview. |
| tool-proposal | proven here | native (`mode`) | the propose_write tool step asked for Harness report.md through a Need. Fixture: native-fixture: the Diomedes loop’s fixed local script (loopFixtureAdapter, delegateFixtureAdapter). |
| approval | proven here | native (`mode`) | the exact approval, given after the restart, was recorded. Fixture: native-fixture: the Diomedes loop’s fixed local script (loopFixtureAdapter, delegateFixtureAdapter). |
| steer | unsupported (declared) | unsupported (`commands.steer`) | Declared unsupported: No steering channel is proven on this route. |
| queue | declared, not proven here | host (`commands.follow-up`) | Declared host. No scenario in this run exercises it. |
| stop | unsupported (declared) | unsupported (`commands.interrupt`) | Declared unsupported: There is no in-band interrupt; run-level cancel is the only stop, and it parks non-idempotent work for reconciliation. |
| resume | proven here | native (`commands.resume`) | after a restart the reopened Core continued the run from its record; the 2 turns observed before it read the same after it (true). Fixture: native-fixture: the Diomedes loop’s fixed local script (loopFixtureAdapter, delegateFixtureAdapter). |
| retry | declared, not proven here | native (`commands.retry`) | Declared native. No scenario in this run exercises it. |
| fork | declared, not proven here | native (`commands.fork`) | Declared native. No scenario in this run exercises it. |
| verification | proven here | host (`mode`) | H17 at the finish: Verified — 2 declared checks passed against 1 exact file version. Fixture: native-fixture: the Diomedes loop’s fixed local script (loopFixtureAdapter, delegateFixtureAdapter). |
| context-accounting | proven here | host (`mode`) | H18 account: 7 sections, 950 estimated tokens (utf8-bytes/4). Fixture: native-fixture: the Diomedes loop’s fixed local script (loopFixtureAdapter, delegateFixtureAdapter). |

### `codex-report`

harness-agent, engine `codex` 0.153.4, authentication native-sign-in, tested with 0.153.4.

| Capability | State | Declared | Why |
|---|---|---|---|
| turn | declared, not proven here | native (`commands.start`) | Declared native. Needs the real codex 0.153.4 binary, signed in; no fixture of it drives this capability through Core here. |
| stream | unsupported (declared) | unsupported (`streaming.transientPreview`) | Declared unsupported: The route declares no transient preview. |
| tool-proposal | declared, not proven here | native (`mode`) | Declared native. Needs the real codex 0.153.4 binary, signed in; no fixture of it drives this capability through Core here. |
| approval | declared, not proven here | native (`mode`) | Declared native. Needs the real codex 0.153.4 binary, signed in; no fixture of it drives this capability through Core here. |
| steer | unsupported (declared) | unsupported (`commands.steer`) | Declared unsupported: No steering channel is proven on this route. |
| queue | declared, not proven here | host (`commands.follow-up`) | Declared host. Needs the real codex 0.153.4 binary, signed in; no fixture of it drives this capability through Core here. |
| stop | unsupported (declared) | unsupported (`commands.interrupt`) | Declared unsupported: There is no in-band interrupt; run-level cancel is the only stop, and it parks non-idempotent work for reconciliation. |
| resume | declared, not proven here | native (`commands.resume`) | Declared native. Needs the real codex 0.153.4 binary, signed in; no fixture of it drives this capability through Core here. |
| retry | declared, not proven here | native (`commands.retry`) | Declared native. Needs the real codex 0.153.4 binary, signed in; no fixture of it drives this capability through Core here. |
| fork | declared, not proven here | native (`commands.fork`) | Declared native. Needs the real codex 0.153.4 binary, signed in; no fixture of it drives this capability through Core here. |
| verification | declared, not proven here | host (`mode`) | Declared host. Needs the real codex 0.153.4 binary, signed in; no fixture of it drives this capability through Core here. |
| context-accounting | declared, not proven here | host (`mode`) | Declared host. Needs the real codex 0.153.4 binary, signed in; no fixture of it drives this capability through Core here. |

### `harness-runtime`

harness-agent, engine `diomedes-harness` 1, authentication none, tested with nothing external.

| Capability | State | Declared | Why |
|---|---|---|---|
| turn | declared, not proven here | native (`commands.start`) | Declared native. No scenario in this run exercises it. |
| stream | unsupported (declared) | unsupported (`streaming.transientPreview`) | Declared unsupported: The route declares no transient preview. |
| tool-proposal | declared, not proven here | native (`mode`) | Declared native. No scenario in this run exercises it. |
| approval | declared, not proven here | native (`mode`) | Declared native. No scenario in this run exercises it. |
| steer | unsupported (declared) | unsupported (`commands.steer`) | Declared unsupported: No steering channel is proven on this route. |
| queue | declared, not proven here | host (`commands.follow-up`) | Declared host. No scenario in this run exercises it. |
| stop | unsupported (declared) | unsupported (`commands.interrupt`) | Declared unsupported: There is no in-band interrupt; run-level cancel is the only stop, and it parks non-idempotent work for reconciliation. |
| resume | declared, not proven here | native (`commands.resume`) | Declared native. No scenario in this run exercises it. |
| retry | declared, not proven here | native (`commands.retry`) | Declared native. No scenario in this run exercises it. |
| fork | declared, not proven here | native (`commands.fork`) | Declared native. No scenario in this run exercises it. |
| verification | declared, not proven here | host (`mode`) | Declared host. No scenario in this run exercises it. |
| context-accounting | declared, not proven here | host (`mode`) | Declared host. No scenario in this run exercises it. |

### `sample`

native-worker, engine `sample` 0.1.0, authentication none, tested with nothing external.

| Capability | State | Declared | Why |
|---|---|---|---|
| turn | proven here | native (`commands.start`) | the staged run started and finished (done). Fixture: sample: the staged worker inside Core. |
| stream | unsupported (declared) | unsupported (`streaming.transientPreview`) | Declared unsupported: The route declares no transient preview. |
| tool-proposal | proven here | host (`mode`) | the run proposed “add a file called Sample work notes.md to the project folder” as a Need before the file existed. Fixture: sample: the staged worker inside Core. |
| approval | proven here | host (`mode`) | declining “add a file called Sample work notes.md to the project folder” left Sample work notes.md unwritten (exists: false). Fixture: sample: the staged worker inside Core. |
| steer | unsupported (declared) | unsupported (`commands.steer`) | Declared unsupported, and the refusal was observed: steer was refused: A staged run has no steering channel. Queue the message to send it after this turn. Fixture: sample: the staged worker inside Core. |
| queue | unsupported (declared) | unsupported (`commands.follow-up`) | Declared unsupported: A staged run has no follow-up channel. |
| stop | unsupported (declared) | unsupported (`commands.interrupt`) | Declared unsupported, and the refusal was observed: stop was refused: This route cannot stop only the request in flight: Stop is the only interruption. Fixture: sample: the staged worker inside Core. |
| resume | unsupported (declared) | unsupported (`commands.resume`) | Declared unsupported, and the refusal was observed: resume was refused: A stopped staged run restarts from the beginning. Fixture: sample: the staged worker inside Core. |
| retry | unsupported (declared) | unsupported (`commands.retry`) | Declared unsupported, and the refusal was observed: retry was refused: A staged run has no mid-run retry. Fixture: sample: the staged worker inside Core. |
| fork | unsupported (declared) | unsupported (`commands.fork`) | Declared unsupported, and the refusal was observed: fork was refused: A staged run has no lineage. Fixture: sample: the staged worker inside Core. |
| verification | proven here | host (`mode`) | Verified: 1 declared check passed against 4 exact file versions. Fixture: sample: the staged worker inside Core. |
| context-accounting | unsupported (declared) | unsupported (`mode`) | Declared unsupported: The engine builds and manages its own context; Diomedes measures none of it. |

### `codex`

external-session, engine `codex` 0.153.4, authentication native-sign-in, tested with 0.153.4.

| Capability | State | Declared | Why |
|---|---|---|---|
| turn | proven here | native (`commands.start`) | the retried turn answered with a proposal for Fall menu.md. Fixture: codex: scripted Work call (nativeGenerator) that fails once, then proposes. |
| stream | unsupported (declared) | unsupported (`streaming.transientPreview`) | Declared unsupported: The route declares no transient preview. |
| tool-proposal | proven here | host (`mode`) | an exact proposal waits on a Need with digests (true). Fixture: codex: scripted Work call (nativeGenerator) that fails once, then proposes. |
| approval | proven here | host (`mode`) | the exact approval was recorded and the file holds the approved bytes (true). Fixture: codex: scripted Work call (nativeGenerator) that fails once, then proposes. |
| steer | unsupported (declared) | unsupported (`commands.steer`) | Declared unsupported, and the refusal was observed: steer on a running Codex turn with no recorded thread: refused (unsupported) Fixture: codex: scripted Work call (nativeGenerator) that holds until its signal aborts. |
| queue | unsupported (declared) | unsupported (`commands.follow-up`) | Declared unsupported: Each ask is a fresh turn; the route does not expose continuing the same one. |
| stop | proven here | native (`commands.interrupt`) | Stop (generation) was applied and acknowledged; the in-flight call saw its abort (true); no proposal was written (true); the run is working. Fixture: codex: scripted Work call (nativeGenerator) that holds until its signal aborts. |
| resume | unsupported (declared) | unsupported (`commands.resume`) | Declared unsupported: A turn does not outlive its dispatch. |
| retry | proven here | host (`commands.retry`) | retry was applied, attempt 2, as Scc2c3b94c176. Fixture: codex: scripted Work call (nativeGenerator) that fails once, then proposes. |
| fork | unsupported (declared) | unsupported (`commands.fork`) | Declared unsupported: The ask route has no lineage. |
| verification | proven here | host (`mode`) | H17 on the retried run: Verified — 2 declared checks passed against 1 exact file version. Fixture: codex: scripted Work call (nativeGenerator) that fails once, then proposes. |
| context-accounting | unsupported (declared) | unsupported (`mode`) | Declared unsupported: The engine builds and manages its own context; Diomedes measures none of it. |

### `claude-code`

single-turn-text, engine `claude-code` 2.1.252, authentication native-sign-in, tested with 2.1.252.

| Capability | State | Declared | Why |
|---|---|---|---|
| turn | declared, not proven here | native (`commands.start`) | Declared native. Needs the real claude-code 2.1.252 binary, signed in; no fixture of it drives this capability through Core here. |
| stream | declared, not proven here | native (`streaming.transientPreview`) | Declared native. Needs the real claude-code 2.1.252 binary, signed in; no fixture of it drives this capability through Core here. |
| tool-proposal | declared, not proven here | host (`mode`) | Declared host. Needs the real claude-code 2.1.252 binary, signed in; no fixture of it drives this capability through Core here. |
| approval | declared, not proven here | host (`mode`) | Declared host. Needs the real claude-code 2.1.252 binary, signed in; no fixture of it drives this capability through Core here. |
| steer | unsupported (declared) | unsupported (`commands.steer`) | Declared unsupported: The text route has no in-band steering channel. |
| queue | unsupported (declared) | unsupported (`commands.follow-up`) | Declared unsupported: Each request is one turn over a fresh session; a follow-up is a new request. |
| stop | declared, not proven here | native (`commands.interrupt`) | Declared native. Needs the real claude-code 2.1.252 binary, signed in; no fixture of it drives this capability through Core here. |
| resume | unsupported (declared) | unsupported (`commands.resume`) | Declared unsupported: Nothing outlives the request process; there is no provider session to reattach. |
| retry | declared, not proven here | host (`commands.retry`) | Declared host. Needs the real claude-code 2.1.252 binary, signed in; no fixture of it drives this capability through Core here. |
| fork | unsupported (declared) | unsupported (`commands.fork`) | Declared unsupported: A single request has no lineage to fork. |
| verification | declared, not proven here | host (`mode`) | Declared host. Needs the real claude-code 2.1.252 binary, signed in; no fixture of it drives this capability through Core here. |
| context-accounting | unsupported (declared) | unsupported (`mode`) | Declared unsupported: The engine builds and manages its own context; Diomedes measures none of it. |

### `opencode`

single-turn-text, engine `opencode` 1.18.4, authentication native-sign-in, tested with 1.18.4.

| Capability | State | Declared | Why |
|---|---|---|---|
| turn | declared, not proven here | native (`commands.start`) | Declared native. Needs the real opencode 1.18.4 binary, signed in; no fixture of it drives this capability through Core here. |
| stream | declared, not proven here | native (`streaming.transientPreview`) | Declared native. Needs the real opencode 1.18.4 binary, signed in; no fixture of it drives this capability through Core here. |
| tool-proposal | declared, not proven here | host (`mode`) | Declared host. Needs the real opencode 1.18.4 binary, signed in; no fixture of it drives this capability through Core here. |
| approval | declared, not proven here | host (`mode`) | Declared host. Needs the real opencode 1.18.4 binary, signed in; no fixture of it drives this capability through Core here. |
| steer | unsupported (declared) | unsupported (`commands.steer`) | Declared unsupported: The text route has no in-band steering channel. |
| queue | unsupported (declared) | unsupported (`commands.follow-up`) | Declared unsupported: Each request is one turn over a fresh session; a follow-up is a new request. |
| stop | declared, not proven here | native (`commands.interrupt`) | Declared native. Needs the real opencode 1.18.4 binary, signed in; no fixture of it drives this capability through Core here. |
| resume | unsupported (declared) | unsupported (`commands.resume`) | Declared unsupported: Nothing outlives the request process; there is no provider session to reattach. |
| retry | declared, not proven here | host (`commands.retry`) | Declared host. Needs the real opencode 1.18.4 binary, signed in; no fixture of it drives this capability through Core here. |
| fork | unsupported (declared) | unsupported (`commands.fork`) | Declared unsupported: A single request has no lineage to fork. |
| verification | declared, not proven here | host (`mode`) | Declared host. Needs the real opencode 1.18.4 binary, signed in; no fixture of it drives this capability through Core here. |
| context-accounting | unsupported (declared) | unsupported (`mode`) | Declared unsupported: The engine builds and manages its own context; Diomedes measures none of it. |

### `oh-my-pi`

single-turn-text, engine `oh-my-pi` 18.0.6, authentication native-sign-in, tested with 18.0.6.

| Capability | State | Declared | Why |
|---|---|---|---|
| turn | declared, not proven here | native (`commands.start`) | Declared native. Needs the real oh-my-pi 18.0.6 binary, signed in; no fixture of it drives this capability through Core here. |
| stream | declared, not proven here | native (`streaming.transientPreview`) | Declared native. Needs the real oh-my-pi 18.0.6 binary, signed in; no fixture of it drives this capability through Core here. |
| tool-proposal | declared, not proven here | host (`mode`) | Declared host. Needs the real oh-my-pi 18.0.6 binary, signed in; no fixture of it drives this capability through Core here. |
| approval | declared, not proven here | host (`mode`) | Declared host. Needs the real oh-my-pi 18.0.6 binary, signed in; no fixture of it drives this capability through Core here. |
| steer | unsupported (declared) | unsupported (`commands.steer`) | Declared unsupported: The text route has no in-band steering channel. |
| queue | unsupported (declared) | unsupported (`commands.follow-up`) | Declared unsupported: Each request is one turn over a fresh session; a follow-up is a new request. |
| stop | declared, not proven here | native (`commands.interrupt`) | Declared native. Needs the real oh-my-pi 18.0.6 binary, signed in; no fixture of it drives this capability through Core here. |
| resume | unsupported (declared) | unsupported (`commands.resume`) | Declared unsupported: Nothing outlives the request process; there is no provider session to reattach. |
| retry | declared, not proven here | host (`commands.retry`) | Declared host. Needs the real oh-my-pi 18.0.6 binary, signed in; no fixture of it drives this capability through Core here. |
| fork | unsupported (declared) | unsupported (`commands.fork`) | Declared unsupported: A single request has no lineage to fork. |
| verification | declared, not proven here | host (`mode`) | Declared host. Needs the real oh-my-pi 18.0.6 binary, signed in; no fixture of it drives this capability through Core here. |
| context-accounting | unsupported (declared) | unsupported (`mode`) | Declared unsupported: The engine builds and manages its own context; Diomedes measures none of it. |

### `cursor`

single-turn-text, engine `cursor` 2026.08.11, authentication native-sign-in, tested with 2026.08.11.

| Capability | State | Declared | Why |
|---|---|---|---|
| turn | proven here | native (`commands.start`) | a direct Ask on the text route answered 200: “answer after 0 earlier turns” — a fresh session, no history. Fixture: cursor: tests/fixtures/acp-agent.mjs, a real ACP v1 agent process standing in for the Cursor CLI. |
| stream | proven here | native (`streaming.transientPreview`) | 1 transient text deltas reached the event stream for the direct Ask. Fixture: cursor: tests/fixtures/acp-agent.mjs, a real ACP v1 agent process standing in for the Cursor CLI. |
| tool-proposal | declared, not proven here | host (`mode`) | Declared host. Needs the real cursor 2026.08.11 binary, signed in; no fixture of it drives this capability through Core here. |
| approval | declared, not proven here | host (`mode`) | Declared host. Needs the real cursor 2026.08.11 binary, signed in; no fixture of it drives this capability through Core here. |
| steer | unsupported (declared) | unsupported (`commands.steer`) | Declared unsupported: The text route has no in-band steering channel. |
| queue | unsupported (declared) | unsupported (`commands.follow-up`) | Declared unsupported: Each request is one turn over a fresh session; a follow-up is a new request. |
| stop | declared, not proven here | native (`commands.interrupt`) | Declared native. Needs the real cursor 2026.08.11 binary, signed in; no fixture of it drives this capability through Core here. |
| resume | unsupported (declared) | unsupported (`commands.resume`) | Declared unsupported: Nothing outlives the request process; there is no provider session to reattach. |
| retry | declared, not proven here | host (`commands.retry`) | Declared host. Needs the real cursor 2026.08.11 binary, signed in; no fixture of it drives this capability through Core here. |
| fork | unsupported (declared) | unsupported (`commands.fork`) | Declared unsupported: A single request has no lineage to fork. |
| verification | declared, not proven here | host (`mode`) | Declared host. Needs the real cursor 2026.08.11 binary, signed in; no fixture of it drives this capability through Core here. |
| context-accounting | unsupported (declared) | unsupported (`mode`) | Declared unsupported: The engine builds and manages its own context; Diomedes measures none of it. |

### `devin`

single-turn-text, engine `devin` 3000.10.23, authentication native-sign-in, tested with 3000.10.23.

| Capability | State | Declared | Why |
|---|---|---|---|
| turn | declared, not proven here | native (`commands.start`) | Declared native. Needs the real devin 3000.10.23 binary, signed in; no fixture of it drives this capability through Core here. |
| stream | declared, not proven here | native (`streaming.transientPreview`) | Declared native. Needs the real devin 3000.10.23 binary, signed in; no fixture of it drives this capability through Core here. |
| tool-proposal | declared, not proven here | host (`mode`) | Declared host. Needs the real devin 3000.10.23 binary, signed in; no fixture of it drives this capability through Core here. |
| approval | declared, not proven here | host (`mode`) | Declared host. Needs the real devin 3000.10.23 binary, signed in; no fixture of it drives this capability through Core here. |
| steer | unsupported (declared) | unsupported (`commands.steer`) | Declared unsupported: The text route has no in-band steering channel. |
| queue | unsupported (declared) | unsupported (`commands.follow-up`) | Declared unsupported: Each request is one turn over a fresh session; a follow-up is a new request. |
| stop | declared, not proven here | native (`commands.interrupt`) | Declared native. Needs the real devin 3000.10.23 binary, signed in; no fixture of it drives this capability through Core here. |
| resume | unsupported (declared) | unsupported (`commands.resume`) | Declared unsupported: Nothing outlives the request process; there is no provider session to reattach. |
| retry | declared, not proven here | host (`commands.retry`) | Declared host. Needs the real devin 3000.10.23 binary, signed in; no fixture of it drives this capability through Core here. |
| fork | unsupported (declared) | unsupported (`commands.fork`) | Declared unsupported: A single request has no lineage to fork. |
| verification | declared, not proven here | host (`mode`) | Declared host. Needs the real devin 3000.10.23 binary, signed in; no fixture of it drives this capability through Core here. |
| context-accounting | unsupported (declared) | unsupported (`mode`) | Declared unsupported: The engine builds and manages its own context; Diomedes measures none of it. |

### `aws-bedrock`

harness-agent, engine `aws-bedrock` ai@7.0.107+@ai-sdk/openai@4.0.71, authentication host-credential, tested with ai@7.0.107+@ai-sdk/openai@4.0.71.

| Capability | State | Declared | Why |
|---|---|---|---|
| turn | proven here | host (`commands.start`) | the turn answered 200: “Answered H20-FIRST.”. Fixture: aws-bedrock: captured-shape Responses stream (tests/fixtures/model-api-streams.ts) below the AI SDK. |
| stream | proven here | native (`streaming.transientPreview`) | 5 transient text deltas reached the Console’s event stream for this thread. Fixture: aws-bedrock: captured-shape Responses stream (tests/fixtures/model-api-streams.ts) below the AI SDK. |
| tool-proposal | proven here | native (`mode`) | the conversation proposed acting (proposed); selected, Work on aws-bedrock proposed Linen checklist.md as an exact Need before the file existed. Fixture: aws-bedrock: captured-shape Responses stream (tests/fixtures/model-api-streams.ts) below the AI SDK. |
| approval | proven here | native (`mode`) | the exact approval was recorded; the file holds the approved bytes (true); the run ended done. Fixture: aws-bedrock: captured-shape Responses stream (tests/fixtures/model-api-streams.ts) below the AI SDK. |
| steer | unsupported (declared) | unsupported (`commands.steer`) | Declared unsupported: A running call cannot be redirected; stop it and send a new message. |
| queue | proven here | host (`commands.follow-up`) | a follow-up message was answered (200) as a new bounded turn whose request carried the earlier message (true). Fixture: aws-bedrock: captured-shape Responses stream (tests/fixtures/model-api-streams.ts) below the AI SDK. |
| stop | proven here | host (`commands.interrupt`) | interrupt answered 200; the turn ended 200 with interrupted=true. Fixture: aws-bedrock: captured-shape Responses stream (tests/fixtures/model-api-streams.ts) below the AI SDK. |
| resume | proven here | host (`commands.resume`) | after a restart the next message was answered (200) and its request carried the conversation from the durable record (true). Fixture: aws-bedrock: captured-shape Responses stream (tests/fixtures/model-api-streams.ts) below the AI SDK. |
| retry | declared, not proven here | host (`commands.retry`) | Declared host. Needs a live aws-bedrock credential or a transport fixture that exercises this capability; none ran here. |
| fork | unsupported (declared) | unsupported (`commands.fork`) | Declared unsupported: Forking a model-API conversation is not supported. |
| verification | proven here | host (`mode`) | H17 on the Work run: Verified — 1 declared check passed against 1 exact file version. Fixture: aws-bedrock: captured-shape Responses stream (tests/fixtures/model-api-streams.ts) below the AI SDK. |
| context-accounting | proven here | host (`mode`) | H18 account on the answered turn: 7 sections, 1089 estimated tokens, provider reported 900 input tokens. Fixture: aws-bedrock: captured-shape Responses stream (tests/fixtures/model-api-streams.ts) below the AI SDK. |

### `azure-openai`

harness-agent, engine `azure-openai` ai@7.0.107+@ai-sdk/azure@4.0.75+@ai-sdk/openai@4.0.71, authentication host-credential, tested with ai@7.0.107+@ai-sdk/azure@4.0.75+@ai-sdk/openai@4.0.71.

| Capability | State | Declared | Why |
|---|---|---|---|
| turn | declared, not proven here | host (`commands.start`) | Declared host. Needs a live azure-openai credential or a transport fixture that exercises this capability; none ran here. |
| stream | declared, not proven here | native (`streaming.transientPreview`) | Declared native. Needs a live azure-openai credential or a transport fixture that exercises this capability; none ran here. |
| tool-proposal | declared, not proven here | native (`mode`) | Declared native. Needs a live azure-openai credential or a transport fixture that exercises this capability; none ran here. |
| approval | declared, not proven here | native (`mode`) | Declared native. Needs a live azure-openai credential or a transport fixture that exercises this capability; none ran here. |
| steer | unsupported (declared) | unsupported (`commands.steer`) | Declared unsupported: A running call cannot be redirected; stop it and send a new message. |
| queue | declared, not proven here | host (`commands.follow-up`) | Declared host. Needs a live azure-openai credential or a transport fixture that exercises this capability; none ran here. |
| stop | declared, not proven here | host (`commands.interrupt`) | Declared host. Needs a live azure-openai credential or a transport fixture that exercises this capability; none ran here. |
| resume | declared, not proven here | host (`commands.resume`) | Declared host. Needs a live azure-openai credential or a transport fixture that exercises this capability; none ran here. |
| retry | declared, not proven here | host (`commands.retry`) | Declared host. Needs a live azure-openai credential or a transport fixture that exercises this capability; none ran here. |
| fork | unsupported (declared) | unsupported (`commands.fork`) | Declared unsupported: Forking a model-API conversation is not supported. |
| verification | declared, not proven here | host (`mode`) | Declared host. Needs a live azure-openai credential or a transport fixture that exercises this capability; none ran here. |
| context-accounting | declared, not proven here | host (`mode`) | Declared host. Needs a live azure-openai credential or a transport fixture that exercises this capability; none ran here. |

### `openrouter`

harness-agent, engine `openrouter` ai@7.0.107+@openrouter/ai-sdk-provider@3.1.0, authentication host-credential, tested with ai@7.0.107+@openrouter/ai-sdk-provider@3.1.0.

| Capability | State | Declared | Why |
|---|---|---|---|
| turn | declared, not proven here | host (`commands.start`) | Declared host. Needs a live openrouter credential or a transport fixture that exercises this capability; none ran here. |
| stream | declared, not proven here | native (`streaming.transientPreview`) | Declared native. Needs a live openrouter credential or a transport fixture that exercises this capability; none ran here. |
| tool-proposal | declared, not proven here | native (`mode`) | Declared native. Needs a live openrouter credential or a transport fixture that exercises this capability; none ran here. |
| approval | declared, not proven here | native (`mode`) | Declared native. Needs a live openrouter credential or a transport fixture that exercises this capability; none ran here. |
| steer | unsupported (declared) | unsupported (`commands.steer`) | Declared unsupported: A running call cannot be redirected; stop it and send a new message. |
| queue | declared, not proven here | host (`commands.follow-up`) | Declared host. Needs a live openrouter credential or a transport fixture that exercises this capability; none ran here. |
| stop | declared, not proven here | host (`commands.interrupt`) | Declared host. Needs a live openrouter credential or a transport fixture that exercises this capability; none ran here. |
| resume | declared, not proven here | host (`commands.resume`) | Declared host. Needs a live openrouter credential or a transport fixture that exercises this capability; none ran here. |
| retry | declared, not proven here | host (`commands.retry`) | Declared host. Needs a live openrouter credential or a transport fixture that exercises this capability; none ran here. |
| fork | unsupported (declared) | unsupported (`commands.fork`) | Declared unsupported: Forking a model-API conversation is not supported. |
| verification | declared, not proven here | host (`mode`) | Declared host. Needs a live openrouter credential or a transport fixture that exercises this capability; none ran here. |
| context-accounting | declared, not proven here | host (`mode`) | Declared host. Needs a live openrouter credential or a transport fixture that exercises this capability; none ran here. |

### `google-vertex`

harness-agent, engine `google-vertex` ai@7.0.107+@ai-sdk/google-vertex@5.0.90+@ai-sdk/google@4.0.77+google-auth-library@10.9.1, authentication host-credential, tested with ai@7.0.107+@ai-sdk/google-vertex@5.0.90+@ai-sdk/google@4.0.77+google-auth-library@10.9.1.

| Capability | State | Declared | Why |
|---|---|---|---|
| turn | declared, not proven here | host (`commands.start`) | Declared host. Needs a live google-vertex credential or a transport fixture that exercises this capability; none ran here. |
| stream | declared, not proven here | native (`streaming.transientPreview`) | Declared native. Needs a live google-vertex credential or a transport fixture that exercises this capability; none ran here. |
| tool-proposal | declared, not proven here | native (`mode`) | Declared native. Needs a live google-vertex credential or a transport fixture that exercises this capability; none ran here. |
| approval | declared, not proven here | native (`mode`) | Declared native. Needs a live google-vertex credential or a transport fixture that exercises this capability; none ran here. |
| steer | unsupported (declared) | unsupported (`commands.steer`) | Declared unsupported: A running call cannot be redirected; stop it and send a new message. |
| queue | declared, not proven here | host (`commands.follow-up`) | Declared host. Needs a live google-vertex credential or a transport fixture that exercises this capability; none ran here. |
| stop | declared, not proven here | host (`commands.interrupt`) | Declared host. Needs a live google-vertex credential or a transport fixture that exercises this capability; none ran here. |
| resume | declared, not proven here | host (`commands.resume`) | Declared host. Needs a live google-vertex credential or a transport fixture that exercises this capability; none ran here. |
| retry | declared, not proven here | host (`commands.retry`) | Declared host. Needs a live google-vertex credential or a transport fixture that exercises this capability; none ran here. |
| fork | unsupported (declared) | unsupported (`commands.fork`) | Declared unsupported: Forking a model-API conversation is not supported. |
| verification | declared, not proven here | host (`mode`) | Declared host. Needs a live google-vertex credential or a transport fixture that exercises this capability; none ran here. |
| context-accounting | declared, not proven here | host (`mode`) | Declared host. Needs a live google-vertex credential or a transport fixture that exercises this capability; none ran here. |

