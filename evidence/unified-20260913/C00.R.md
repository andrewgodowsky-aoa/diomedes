# C00.R independent acceptance and repair handoff

Reviewed on 2026-09-13 by Codex gpt-6-astra. The verdict is **FAILED** for C00.I candidate `a0341639d3a918d0bb87753758f600a37f4beb3e`. Contract revision `2026-09-13.1` remains proposed; no revision is approved by this review. Fable must repair C00 before executing **03a B00.I** or **03b H01.I**. Read-only preparation can continue, but no implementation prerequisite subset is released.

## Candidate and ownership

- Original base: `212106e75a710242e74620c3c5947aef4bbeef23`.
- Producer worktree: `F:/Diomedes/diomedes-wt/c00-rebaseline-20260913`, clean at `a0341639d3a918d0bb87753758f600a37f4beb3e` when inspected.
- Integrated app source: `03a1b800bca95e23eaa38a25bb17a93d38b97847`; also observed on `origin/main` with `git ls-remote`. The C00 executable source blobs are unchanged there. Its AGENTS.md additionally updates FIL-02 decision wording.
- Site source: `cf784de56a5102ffb9948878eaeee31af51f7e10`.
- Independent worktree: `F:/Diomedes/diomedes-wt/astra-c00-review-20260913`, detached at the original base, with C00 reconstructed as uncommitted changes. The main checkout had 17 existing dirty status entries and was preserved.
- One common coordination root: `F:/Diomedes/diomedes/.git/diomedes-coordination/unified-20260913`. Reviewer claim: `claim_mtzfnna9_958a1b27`. A persistent owner process supplied a stable host, PID and process-start identity. The shared heavy slot covered fresh tests and type checking, then was released.
- The independent additions are `tests/c00-independent.test.ts`, this report, `C00.R.json`, and `c00-r-tests.json`. No producer production code, installed application, live account, canonical document, or site was changed by the reviewer. No commit or push was performed.

The submitted patch has SHA-256 `2fc947138f7f4b75e08a254a7199bfed0062aa420cb184736780c862b45de499` and matches its advertised patch hash. It initially reconstructed nine of the ten candidate Git blobs: the in-tree C00.I result was an older version. Applying the metadata-only difference between the two producer commits in the reviewer worktree brought all ten Git blobs to the exact candidate. Every tested executable blob already matched before that correction. The corrected reconstruction patch and both verification records are retained under `F:/Diomedes/deliverables/c00-review-20260913/`.

## Blocking findings

### C00-R1 (P1): aliases and directory claims defeat exclusive path ownership

`scripts/coordination.ts:120`, `:129`, and `:191` normalize separators and one leading `./`, then compare and lock literal strings. They do not resolve dot segments, apply Windows case identity, or detect ancestor/descendant claims. Independent tests C02-C05 reproduce two winners for `tests/a.ts` versus `tests/../tests/a.ts`, allow Astra to claim `SERVER/APP.TS` and `server/../server/app.ts`, and allow a directory and its child file to be claimed by different workers in either order.

This is already relevant to the producer workflow: C00.I claims `evidence/unified-20260913/` as a directory. Two successful claims are therefore not proof of disjoint ownership. Canonicalize validated repository-relative identities before both hot-file checks and locks; reject escapes and ambiguous inputs. Make directory overlap exclusion atomic across contenders, or explicitly disallow directory claims and migrate existing directory work orders to an equivalent enforced representation. A check-then-write scan alone does not resolve the race.

### C00-R2 (P1): an arbitrary handoff string bypasses integrator ownership

At `scripts/coordination.ts:197`, any truthy `handoffFrom` bypasses the hot-file restriction. C06 successfully claims `shared/types.ts` as Astra using `handoffFrom: 'never-issued'`. No handoff record is read or validated.

Require a recorded, applicable handoff with the authorized sender, recipient identity, exact scope and candidate/base association. Reject nonexistent, mismatched and replayed handoffs; preserve the ordinary exclusive-claim conflict. A string naming a supposed handoff cannot itself authorize it.

### C00-R3 (P1): owner checks omit the process identity that the record promises

`releaseClaim` at `scripts/coordination.ts:261` accepts any process with the holder's role. `releaseSlot` at `:348` checks only role and PID. `workMode` at `:411` uses that same incomplete identity. C07-C09 and C11 show a second same-role process releasing a claim, a reused PID or another host releasing a slot, and another process lifetime being allowed to edit the holder's file.

Compare the complete stable owner identity consistently, preserving separately authorized integrator handoffs. Also fix `ownerFromArgs` at `:442-444`: it defaults to the parent PID but derives the start time from the short-lived CLI process. The default must obtain the matching process start or require an explicit stable identity. A timestamp remains identity evidence, never permission to steal an old claim. These are coordination failures; the tests do not demonstrate a breach of the application's tool authorization boundary.

### C00-R4 (P2): the proposed frozen schemas admit contradictory facts

At `shared/contract-revision.ts:173-194`, the profile schema admits both a non-null reported model with `source: 'not-recorded'` and a null reported model with `source: 'runtime'`. S03-S04 reject those states independently. They contradict the field's documented meaning and the existing attribution constructor at `shared/attribution.ts:63-64`.

At `shared/contract-revision.ts:87-106`, authoritative run and event-run identifiers accept `run/other`; the current `FileRunStore` rejects the same identifier at `server/harness/run-store.ts:27-30`. S05 reproduces the mismatch without attempting any filesystem injection. Reuse an agreed shared identifier contract and make source/reporting consistency explicit before consumers freeze against these schemas. Review corresponding continuation references for consistency as part of the same small repair.

### C00-R5 (P2): the returned manifest does not reconstruct its advertised candidate

The submitted patch carries the earlier in-tree C00.I.json, and the external manifest advertises that file's SHA-256 as `3ed5e22121616bd5b2c9ed1a59d6abd6204a2c461b004507ceaa7c7e025b1e13`. The candidate and producer checkout contain `d4f38a8b80059efb45d7991f947ce0bc774a64978b57d4b2c78a6055a5eac7c1`. This mismatch persists on the producer checkout; it is not a Windows line-ending difference. The other apparent physical-byte hash differences in the reviewer reconstruction are CRLF/LF differences, and their Git blobs match.

Return one reproducible candidate manifest after finalizing the payload. Keep the outer manifest separate from files whose hashes it records, define the byte or Git-blob hashing convention, and avoid self-referential result hashes. Correct the stale 30-test status text to the observed 32-test producer result. The corrected local reconstruction in this review is evidence, not an amendment to Fable's submitted result.

## Fresh validation

All test commands below ran in the independent worktree against the candidate's executable blobs. Logs and machine-readable Vitest output are in `F:/Diomedes/deliverables/c00-review-20260913/`.

| Check | Observed result | Evidence |
| --- | --- | --- |
| Producer contract and coordination suites | 32 passed | `independent-tests-final.json`, `independent-tests-final.log` |
| Independent acceptance suite | 27 cases: 14 passed, 13 failed | Same files; `tests/c00-independent.test.ts` |
| Existing runtime, negative, lifecycle and provider-outcome suites | 118 passed across four files | `existing-runtime-tests.json`, `existing-runtime-tests.log` |
| TypeScript | `tsc --noEmit`, exit 0 | `typecheck.log` |
| Exact candidate reconstruction | 10/10 changed Git blobs match after metadata-only correction | `candidate-verification-final.json` |

Commands actually run:

```powershell
.\node_modules\.bin\vitest.cmd run tests/coordination.test.ts tests/contract-revision.test.ts tests/c00-independent.test.ts --maxWorkers=2 --reporter=default --reporter=json --outputFile.json=F:/Diomedes/deliverables/c00-review-20260913/independent-tests-final.json
.\node_modules\.bin\vitest.cmd run tests/harness-provider-outcomes.test.ts tests/harness-lifecycle.test.ts tests/harness.test.ts tests/harness-negative.test.ts --maxWorkers=2 --reporter=default --reporter=json --outputFile.json=F:/Diomedes/deliverables/c00-review-20260913/existing-runtime-tests.json
.\node_modules\.bin\tsc.cmd --noEmit
```

The 13 independent failures are C02; the two C03 variants; C04; C05; C06; C07; C08; C09; C11; S03; S04; and S05. They are retained as failing acceptance fixtures for the producer repair. No test was weakened to obtain a pass. The final focused run repeats the same results after C12 was made portable through Git worktree discovery and C13 was strengthened to issue both heavy-slot requests concurrently. Type checking also passed after those fixture changes. The earlier test output is retained separately.

The independent passing cases establish identical-file claim contention, slot contention and ordinary owner release, the common root from both checkouts, and missing-partner read-only behavior. Contract cases validate external examples, distinct requested/reported model identity, native-reference requirements, and logged-only steering. A real FileRunStore disk round trip preserves the old run bytes and does not add tools, packs, receipts or native continuation. Real `validateEffect` calls reject revoked grants, advanced identity generation, revoked membership, and an activated pack without an authorization grant.

The current runtime cancellation and late-result tests pass. Native steering delivery racing terminal completion is **not independently certified**: the candidate adds an acknowledgment schema but no native steering transport or composite run/turn transition to exercise. The tests prove that a logged note cannot parse as delivered without a native session; they do not establish the later H01-H08 live transport semantics. Live-provider, browser, packaged, installed-app, publication and deployment gates were not run by this reviewer. Fable's integration-gate report remains producer evidence.

## Coverage and consumer prerequisites

The parent reconciled the delegated source-anchor inspection with a machine-readable comparison in `coverage-reconciliation.json`. All 41 rows match both the original COVERAGE ledger and the unified mapping, with no missing or changed work-item mappings. All 33 original harness items remain. All 52 implementation orders match RUN_ORDER roles, dependencies, priorities, waves and optional flags. The 104-node graph is acyclic, and its 44-wave dependency table is complete and correctly ordered. Optional SDKO, B12 and G01 have no downstream consumers that would make them mandatory.

The producer's baseline counts remain 11 present-and-verified, 18 partial, 11 absent and one externally blocked. Those 11 labels describe the stated deterministic baseline components and their cited tests; they are not 11 fresh C00.R passes or evidence that the complete native conformance scope is finished. In particular, bounded Claude/OpenCode adapter fixtures do not close H03/H04's persistent-session and recovery work. FIL-02's partial classification is explicitly scoped to base `212106e`; its later integrated slice must be assessed against its own source and evidence.

Five supporting release rows remain separate: Windows packaging, code signing, updates/rollback, site publication, and Norton false-positive handling. None was promoted by the offline capability checks. Correct HAR-10's "now frozen" wording to "proposed" until independent acceptance, and normalize `dependency-order.md` to UTF-8: the current file fails UTF-8 decoding at byte 19, although its cp1252-decoded table preserves the graph. These evidence corrections accompany C00-R5; the graph itself adds no separate blocker to 03a or 03b.

The delegated report also identified the ledger's line 151 label error: work orders contain 38 `missing-on-base` anchor entries and one `proposed-new`, not 38 `proposed-new`. The parent accepted that correction and the stale 30/32 test-count correction. It rejected the report's broader inference that all 38 markers prove files are absent: site-prefixed anchors were checked as literal strings in the app worktree, while `F:/Diomedes/diomedes-site/src/data/site.ts`, `src/data/releases.ts`, and `src/pages/` actually exist. Bind site markers to their named repository and real paths before the site work-order handoff. This does not change B00/H01's verified app anchors.

B00 owns completion of the commercial principal/entitlement/credential seam; H01 owns the adapter/event/context interface and SDK extension hooks. Their later implementation is not being demanded as part of C00. Neither may treat the currently proposed schemas or the broken coordination exclusions as accepted prerequisites. Both require C00.R, and no safe implementation subset is released while those shared prerequisites fail.

## Current environment and evidence limits

The repository canonical versions are pillars `2026-09-10.1`, roadmap `2026-09-12.2`, and project memory `2026-09-10.7`. Fresh read-only Google Drive retrieval confirms pillars `2026-09-10.1`, roadmap `2026-09-10.4`, and memory `2026-09-10.4`; `cloud-versions.json` records the document and revision IDs. The known mirror reconciliation remains D01 work. No canonical claims or historical evidence were rewritten.

The installed runtime probe reports shell Node `22.23.2`, Electron `44.2.0` with embedded Node `24.20.0`, Vite `7.3.6`, Vitest `3.2.7`, TypeScript `5.9.3`, Zod `4.5.4`, Express `5.2.1`, and Playwright `1.63.0`. The AI SDK is not installed. `runtime-probe.json` captures an Electron dependency executable run in Node mode. This is not proof of a packaged Diomedes launch or an SDK import in its production boundary.

The parent review used Codex gpt-6-astra. One read-only coverage delegation selected `opencode-go/muse-spark-1.3-contributor` through `ask-opencode.ps1 -Mode ask`; it ended without a completed report after an external-folder access denial. The same bounded lane was retried with `opencode-go/glm-5.3-flash` using copies of the required inputs inside the reviewer worktree. Both routes were explicitly selected from the live local catalog; model-default effort was used because a stronger effort variant was not verified. No extra workers, payer or account were created. These delegated model calls are not Diomedes live-provider acceptance evidence. New fixed and unapproved variable spend remain USD 0; account billing was not measured and no zero-charge execution claim is inferred.

The GLM fallback completed with exit 0. Its prose mistakenly labels itself Muse; the recorded invocation and `plan / glm-5.3-flash` tool header establish the selected route. That self-label was not used as identity evidence. The parent accepted the reproduced mapping facts, qualified the baseline coverage labels, and retained the source and evidence corrections above. The fallback's conditional recommendation to proceed after acceptance does not override the independent failing tests.

## Fable repair handoff

Continue C00.I as a repair pass before 03a or 03b. Re-read current refs, worktree status, claims and journals; use the existing common root and integrator ownership. Preserve the independent integrated slices and the dirty main checkout. Bring the independent regression file into the repair worktree as tests, then fix C00-R1 through C00-R4 in the producer-owned coordination and contract paths. Add positive handoff coverage and competing alias/directory contenders so a serial-only overlap fix cannot appear complete.

Run the 32 producer cases plus all 27 independent cases, relevant affected runtime tests, and type checking. Use the single heavy slot. Return a fresh exact candidate, patch and non-self-referential manifest addressing C00-R5, with coverage corrections identified in this report. Request C00.R again on those exact bytes. The integrator owns the required combined gates on the resulting integration candidate; this failed review grants no publication, commit, push, deployment, app-replacement or spending approval.

No pillar semantics or roadmap completion statuses were changed. The practical roadmap impact is a hold on B00.I and H01.I until the foundational coordination and contract contradictions are repaired and independently accepted.
