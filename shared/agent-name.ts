/**
 * The agent's own name: the one product name the app writes out itself.
 * Lines about what the agent is doing name it, never the model or route
 * serving it; attribution lines still name the model that actually ran.
 */
export const AGENT_NAME = 'Nectovia';
